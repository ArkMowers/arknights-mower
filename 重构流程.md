# 重构流程

> 本文档是 v5 重构的**执行手册**：每一步做什么、怎么算做完。
> 与 `STEPS_OVERVIEW.md` / `STEP_*.md` 的关系：那批文档是**设计规格**（无进度标记），本文档是**执行状态 + 验收标准**。
> 最后更新：S0 已完成并验收（HEAD `3541ad09` + 未提交改动；测试根已迁至仓库根 `tests/`）

---

## 一、当前基线（实测数据）

以下数字均由脚本实测得出，非估算。

> **基线版本**：本表已更新为 **S4 收工后**的值（主控独立复跑逐项核对）。

| 指标 | 起点 | **当前（S4 后）** | 说明 |
|---|---|---|---|
| `scheduler/` 文件数 | 64 | **70** | S1 删 3；S2 +1；S3 +7（`agent_swap_*`）；S4 +1（`navigator_actions.py`） |
| `scheduler/` 总行数 | 5,678 | **5,355** | S1 −743；S2 +19；S3 +361；S4 +40（均为拆分，非新增功能） |
| `raise NotImplementedError` | 10 文件 / 12 处 | 10 文件 / 12 处 | 未迁移的骨架 |
| **`time.sleep(` 残留** | 2 文件 / 17 处 | **0** ✅ | S1 已清零 |
| 文件 > 300 行 | 4 | **1** | 仅 `state.py` 572，属 S5 |
| `ruff check arknights_mower/scheduler` | 38 | **17** | **门禁 G3 判据**；只减不增 |
| 单测 `unittest discover` | 35 | **121** | exit 0；S1 +12，S2 +25，S3 +23，S4 +24。**须连跑 ≥3 次全绿**（§10.2 硬要求 5） |
| 夹具元测试 `tests.harness.selftest` | 13 | **13** | 另立 **G5**（见 §10.1） |
| `python -m compileall -q scheduler` | exit 0 | exit 0 | |
| `ui/dist` | 不存在 | 不存在 | Flask 无法提供网页界面 |

> ⚠️ **历史数字订正**：S0 与 S1 报告中 `scheduler/` 总行数写作 `4928`，那是**迁移中途**的读数。
> 主控实测：S1 后 **4935** → S2 **4954** → S3 **5315** → S4 **5355**。以本表为准。

### 超长文件（按行数）

| 行数 | 文件 | 归属 |
|---|---|---|
| 572 | `scheduler/state.py` | **S5（最后一个）** |

> ✅ S3 消除原 619 行 `services/agent_swap_service.py`（现 113 行）。
> ✅ S4 消除原 450 行 `scheduler/navigator.py`（现 288 行 + `navigator_actions.py` 202 行）。
> **S5 收工后 `scheduler/` 应 0 个超长文件。**

### Executor 迁移状态（9 个具体执行器，不含 `base.py`）

| 状态 | 文件 | 行数 |
|---|---|---|
| ✅ 已迁移 | `shift.py` | 184 |
| ✅ 已迁移 | `workshop.py` | 143 |
| 🔶 部分 | `infra_scan.py` | 86 |
| ⬜ 骨架 | `clue.py` / `correction.py` / `exhaust.py` / `fiammetta.py` / `run_order.py` / `skill.py` | 各 6 |

### Planner 合规状态（10 个 planner，不含 `base.py`）

| 状态 | 文件 | 行数 | 问题 |
|---|---|---|---|
| ✅ 合规 | `idle.py` | 20 | — |
| ✅ 合规 | `infra_scan.py` | 39 | — |
| ✅ 合规 | `workshop.py` | 98 | — |
| ❌ 不合规 | `shift.py` | 165 | `plan(self)`，且一次推入多个任务 |
| ❌ 不合规 | `exhaust.py` | 107 | `plan(self, operator_name)` |
| ❌ 不合规 | `fiammetta.py` | 86 | `plan(self, task_time)` 返回 `bool` |
| ❌ 不合规 | `backup.py` | 38 | `plan(self, timing_value=999)` 返回 `bool` 非 task |
| ❌ 不合规 | `order.py` | 46 | `plan_room(...)` / `plan_all(...)` |
| ⬜ 不合规 | `clue.py` / `skill.py` | 各 6 | `plan(self)` 骨架 |

---

## 二、实机验证环境（已打通）

| 项 | 值 |
|---|---|
| 模拟器 | MuMu Player 12，`127.0.0.1:16416` |
| Python | `.venv\Scripts\python.exe`（CPython 3.12.14） |
| 截图 | `screencap()` 稳定返回 1920×1080 RGB |
| 识别 | 实测可用（首页 / 基建 / 排序界面等均正确识别） |
| 依赖 | `requirements.txt` + `ruff` 已装全 |
| 入口 | `arknights_mower/scheduler/bootstrap.py::run()` |

**注意事项**

- Windows 控制台输出中文需设 `$env:PYTHONIOENCODING="utf-8"`，否则 `recruit_tests.py` 会因 GBK 编码报 `UnicodeEncodeError`（既有问题，非重构引入）。
- `utils/log.py:50` 将 logger 硬编码为 `DEBUG`，识别一帧刷上百行。验证脚本内已降到 `INFO`。
- 已知既有缺陷：`utils/device/device.py:221` 的 `forward --list` 解析会抛 `ValueError: too many values to unpack`。**良性**：被自身 try/except 吞掉，DroidCast 仍正常启动。与重构无关。

---

## 三、阶段 A：打通实机（已完成 A1 / A1.5 / A2）

> 阶段 A 的存在理由：在能实际运行之前，任何"已迁移"的声明都无法验证。

> 📌 **路径变更提示（S0 之后）**：本章及第四/五章是**历史进度记录**，其中的
> `scripts/verify_*.py`、`scripts/probe_select_panel.py`、`arknights_mower/tests/` 路径
> **均已作废**（S0 把测试统一迁到仓库根 `tests/`）。阅读时按此换算，**历史记录本身不改**：
> `scripts/X.py` → `tests/live/X.py`；`arknights_mower/tests/X_tests.py` → `tests/unit/X_tests.py`。
> 现行命令一律以 **§七** 与 **§10.1** 为准。

### A1 — 修复 pause/stop 断线 ✅ **已完成并实机验证**

**问题（v5 回归）**

`bootstrap.run()` 收到的 `pause` 对象是孤岛：
- 未传给 `_build_infra()`，也未传给 `MainLoop`
- `InfraKit.pause` 用了 `field(default_factory=ThreadPauseController)`，**自建第三个实例**
- 结果：UI 的暂停/停止只改动 `server_scheduler.scheduler_pause`，而执行器读的是 `self.infra.pause` → `loop.py` 的 `is_stopped` **永远不可能为真**

**改动文件**

| 文件 | 改动 |
|---|---|
| `scheduler/bootstrap.py` | `_build_infra(v1_device, state, pause)` 显式接收并传递 `pause` |
| `scheduler/infra/__init__.py` | `InfraKit.pause` 改为**必填字段**（删掉 `default_factory`） |
| `scheduler/infra/pause_controller.py` | ABC 补声明 `is_stopped`（原先 `loop.py` 调用了不存在的方法） |
| `scheduler/infra/thread_pause.py` | 重写：`_event` + `_stop_event` 双事件，`wait(seconds)` 可被 stop 立即打断 |
| `scheduler/constants.py` | 新增 `PAUSE_WAKE_SLICE = 0.5` |
| `scheduler/device_port.py` | `__init__(pause)`，`_wait()` 委托给 `pause.wait()` |
| `scheduler/infra/pc_device_port.py` | 透传 `pause` |
| `scheduler/navigator.py` | `pause_controller` 必填（删掉自建兜底） |
| `scheduler/services/agent_swap_service.py` | `pause` 必填 |
| `scheduler/loop.py` | 两处 `time.sleep(1)` → `pause.wait(IDLE_POLL)`；移除 `import time` |

**验收标准（已全部通过）**

| # | 标准 | 命令 / 观察点 | 结果 |
|---|---|---|---|
| 1 | 10 个改动文件语法正确 | `python -m py_compile <files>` | ✅ 10/10 |
| 2 | 全部模块可导入 | `import arknights_mower.scheduler.bootstrap` | ✅ 12/12 |
| 3 | `InfraKit.pause is nav._pause` | 构建后比较 `is` | ✅ True |
| 4 | `pause` 不再有默认值 | 不传 `pause` 构造 `InfraKit` 应报错 | ✅ 必填 |
| 5 | `wait(1.0)` 真实等待约 1 秒 | 计时 | ✅ 1.00s |
| 6 | 暂停时 `wait()` 阻塞 | 暂停后 `wait()` 不返回 | ✅ |
| 7 | `request_stop()` 能解开已暂停的 `wait()` | — | ✅ |
| 8 | stop 能打断 30 秒等待 | 计时 | ✅ 0.30s |
| 9 | 不同实例互不干扰 | 双实例交叉测试 | ✅ |
| 10 | **实机：stop 能真正终止 MainLoop** | `scripts/verify_scheduler_live.py --run` | ✅ **0.00s** |

**关键证据**（实机运行日志）

```
09:02:33,222 MainLoop: enter run_forever
09:02:33,222 no pending tasks, idling...
09:02:37,223 MainLoop stopped (is_stopped)
```

```
[ok] InfraKit 构建完成; pause 同一对象 = True
[1] 运行中: alive=True
[2] pause() 后: is_paused=True alive=True      ← 暂停≠退出，正确
[3] resume() 后: is_paused=False alive=True
[4] request_stop() 后 0.00s: alive=False       ← A1 的验收点
```

> 修复前 `[4]` 恒为 `alive=True`。

---

### A1.5 — 真实换班链路 ✅ **已完成并实机验证**

**做了什么**：用真实设备跑一个 `SHIFT_ON` 任务（单房间，180 秒硬上限）。

**验收标准（已通过）**

| # | 标准 | 结果 |
|---|---|---|
| 1 | 从首页自动导航进基建 | ✅ `INDEX → INFRA_MAIN` |
| 2 | 进入目标房间 | ✅ `room_1_1`，屏幕到 `230 基建放大查看` |
| 3 | 读取房间内干员 | ✅ `RoomReader: room_1_1 [灰毫(18) 野鬃(19) 远牙(18)]` |
| 4 | 进入进驻排序界面 | ✅ `207 干员进驻设施排序界面` |
| 5 | 换班服务正确决策 | ✅ `AgentSwap: room already correct, skip` |
| 6 | 任务正常结束、无异常 | ✅ `state.error=False`，队列清空，`ShiftExecutor.execute()` 正常返回 |

**说明**：本次房间干员与计划一致，`AgentSwapService` 走了"已正确则跳过"分支 —— 这验证了**导航 + 识别 + 读房间 + 决策**全链路，但**未实际执行换人动作**。要验证真正的换人，见 A2。

**未覆盖**：本次未触发任何异常，故"异常不崩溃主循环"这条**在 A1.5 中未被验证**（A1 的 `no pending tasks` 空转路径有覆盖）。

---

### A2 — 验证真正的换人动作 ✅ **已完成并实机验证**

A1.5 走的是"已正确则跳过"分支，换人动作本身未验证。本次用 `dormitory_2` 完成了真实换人。

**做了什么**

```powershell
.venv\Scripts\python.exe scripts\verify_shift_live.py --room dormitory_2 --seconds 240
```

场景：`dormitory_2` 原为 5 人 `流明 琴柳 令 迷迭香 鸿雪`，计划目标为 `流明 琴柳 Free Free Free` —— 即"保留前 2 人、清空后 3 个空位"，会真正触发换人动作。

**验收标准（已通过）**

| # | 标准 | 结果 |
|---|---|---|
| 1 | 导航到目标宿舍 | ✅ 首页 → 201 → 205 → 230，`_detect_room()` = `dormitory_2` |
| 2 | 读取房间内干员 | ✅ `RoomReader: dormitory_2 [流明(24) 琴柳(24) 令(9) 迷迭香(21) 鸿雪(21)]` |
| 3 | `uncheck slots` **非空** | ✅ `uncheck slots=[2, 3, 4]`（证明真的取消了旧干员） |
| 4 | 正确保留目标干员 | ✅ `skip 流明/琴柳 already in slot 0/1`，未误删 |
| 5 | 执行取消勾选动作 | ✅ 3 次点击，`uncheck slot 2 / 3 / 4`，间隔约 70ms |
| 6 | 结束后重新读房间 | ✅ `dormitory_2 [流明 琴柳 空 空 空]` == 计划目标 |
| 7 | `state.error=False` | ✅ 队列清空，`ShiftExecutor.execute()` 正常返回 |
| 8 | 全程无异常 | ✅ 无 `executor failed` / `unhandled error` |

**结论**：`导航 → 识别 → 读房间 → 计算差异 → 执行换人 → 结果正确` 全链路已在真实设备上跑通。

⚠️ 本次成功路径是"减少人数 + 留空位"，由 `uncheck` 达成目标，**未覆盖"从空位补人"的选人路径**（该路径当时是坏的，见 BUG-1）。本次 50 次翻页纯属多余 —— 且因为 BUG-1 恰好什么都没做，反而没造成破坏。**注**：这 50 次翻页的成因是"未筛选的干员总览表长于 `MAX_PAGE=50`"，**不是** `_last_names` 保护缺失（我曾误判，见 BUG-3 的更正）。

**本次未覆盖**：① 补空位选人路径（BUG-1 修复后需重验）；② 生产房间（`room_*`）的换人；③ 排序/筛选切换后换人。

---

### BUG-1 — `_do_scan` 用局部变量，导致"补空位"分支永久失效 ✅ **已修复**

**位置**：`scheduler/services/agent_swap_service.py:193`

**症状**：修复前，代码是

```python
cache = self._operator_list_fn(...)      # 局部变量
...
for name, box in self._cache:            # 永远读到 []
```

`_do_scan` 把识别结果写进**局部变量** `cache`，而 `_do_select` 的补空位分支读的是 `self._cache`（仅在 `__init__` 里初始化为 `[]`，此后从未赋值）。于是该分支恒为空转：**一个位置都补不上**，直接落到翻页逻辑。

**实机证据**：日志中 `switching to free mode` 之后连续 50 次 `swipe page`，**`free tap` 出现 0 次**，最终 `ERROR AgentSwap: max page reached`。

**A/B 验证（mock 场景：需补 2 空位，候选池含 令/迷迭香）**

| | taps | swipes | free_count |
|---|---|---|---|
| 修复前 | **0** | 1（翻页） | 2（没变） |
| 修复后 | **1**（`free tap 令`） | 0 | 1（递减） |

**是回归，不是原始缺陷**：`3541ad09~1` 中 `self._cache` 有 4 处赋值（含 `self._cache = [c for c in self._cache if c[0] != found[0]]`），在 `3541ad09`（"agent swap rewrite with Step pattern"）中被误删。

**修复**：把 `_do_scan` 的局部变量改回 `self._cache`（3 行）。

**修复后的实际收益（实机数据推算）**：本次运行第 0 页就识别出 **12 个合法候选干员**，补 3 个空位只需 3 次点击、**0 次翻页** —— 那 50 次翻页会完全消失。

**验收（已通过）**：`py_compile` OK；`ruff check scheduler/` 仍 38（无新增）；单测全绿（当时 31，当前基线 35）；A/B 对比证明修复前 0 次点击、修复后 1 次。

**遗留问题 1**：`_find_in_cache` / `_find_free_in_cache`（`line 484/490`）当前**无人调用**，是死方法。回归前它们是 free 分支的核心。修复时未接回（当前用就地循环等价实现）。⚠️ 注意：`_find_free_in_cache` 的逻辑与现在的就地循环**完全相同**（都是"取第一个合法干员"），所以**接回它也解决不了下面遗留问题 2**。去留见"待决策事项 #9"。

**遗留问题 2（已实机确认为真缺陷）** 🔴 **已验证**

修复让 free 分支**首次真正开始执行**，因此它原有的候选选择逻辑也变得可触发了。该逻辑是：

```python
for name, box in self._cache:
    if name not in self._agent_list:   # 只判断"是否合法干员"
        continue
    ...  # 取【第一个】合法候选
```

`self._agent_list` 是**全量干员表（415 人）**，**不排除"已在房间内、已勾选"的干员**；排除已勾选的集合是 `self._selected`（本次 = `[流明, 琴柳]`），但 free 分支没有用它。

而 `_do_scan`（line 193）每次都是**全新扫描并覆盖** `self._cache`，所以"点过就从缓存删掉"这种自我保护也不成立（`line 232` 每次点击后返回 `scan` 步骤，重新扫一页新的）。

#### ✅ 实机验证结果（`scripts/probe_select_panel.py`，只读探测）

在 `dormitory_2`（实机 `流明 琴柳 空 空 空`，正待补 3 个 Free）打开选择面板，复现 `_do_scan` 的读数：

```
读到 12 个条目:
  [0] 流明   box=((631, 488), (820, 520))     ← free 分支会点这个
  [1] 琴柳   box=((631, 909), (820, 941))
  [2] Lancet-2
  ...
```

| 判定项 | 结果 |
|---|---|
| `free tap` 会点谁 | **【流明】**（列表第 0 位） |
| 流明是否已在房间内 | **是**（计划第 0 位，已勾选） |
| 琴柳是否在列表中 | **是**（会被误点） |
| 探测是否提交变更 | **否**（脚本第 7 步原路 back 退出，scene 回到 201） |

**结论：风险从"推测"升级为"事实"。** 面板按心情降序排列，而 `流明`/`琴柳` 都是 24 心情（最高），因此必然排在最前 —— free 分支取第一个合法候选时，**点中的就是已经在房间里的干员**。

**性质判定（重要）**：旧代码 `_find_free_in_cache`（`line 490`，现已无人调用）判据**完全相同**（都是"取第一个合法干员"），所以这是**既有设计缺陷**，**不是** BUG-1 修复引入的回归。BUG-1 的修复只是让这段既有逻辑**首次真正开始执行**，从而把缺陷暴露出来。

#### 旧实现为何不会点错 —— 权威对照

旧代码 `base_schedule.py:2932`：

```python
free_list = self.get_free_list(agents)   # ← 关键：先算出"真正空闲"的候选名单
while free_num:
    selected_name, ret = self.scan_agent(free_list, max_agent_count=free_num, ...)
```

`get_free_list()`（`base_schedule.py:2681`）三重过滤：

```python
v.name not in agents  AND  v.operator_type != "high"  AND  v.current_room == ""
                          ^^^^^^^^^^^^^^^^^^^^^^^^^^   ^^^^^^^^^^^^^^^^^^^
                          排除高级干员                  只挑【当前未进驻任何设施】的干员
```

再扣除 `free_blacklist` 和整个计划中已安排的干员名。**即：旧实现只用"空闲干员"做候选，天然不可能点中已在房里的人。** 而 v5 用全量 415 人表做候选，丢失了 `current_room == ""` 这一核心过滤。

**修法方向**：free 分支的候选过滤应改为排除 `self._selected`（房内已有）+ 排除 `self._agents` 中已定向的干员；是否进一步对齐旧实现的"仅空闲（`current_room == ""`）"语义，见"待决策事项 #13"。

**验证方法（已用，可复用）**：`scripts/probe_select_panel.py --room <房间>`
1. 只点一次空位打开面板，**绝不点任何干员**
2. 复现 `_do_scan` 读数，打印 `free tap` 会点谁
3. 原路 `back` 退出，**不提交任何变更**

#### ✅ 修复记录（缺陷 A + B 一并修复，2026-09-15）

**修复前的离线仿真**（`tmp/agent_swap_sim.py`，mock 设备，零游戏交互；面板读数取自实机探测）：

```
场景：dormitory_2 — 计划 [流明, 琴柳, Free, Free, Free]，房内 [流明, 琴柳, 空, 空, 空]

 1. 点干员:流明     ← free 补位第 1 次
 2. 点干员:流明     ← 第 2 次，同一个干员
 3. 点干员:流明     ← 第 3 次，同一个干员
 4. ★清空全部        ← _do_sort 把选中的全部清掉
 5. →槽位0
 6. →槽位1          ← 只补回 流明/琴柳，3 个空位仍是空
```

**修复后的同一仿真**：

```
 1. 点干员:Lancet-2
 2. 点干员:承曦格雷伊
 3. 点干员:温蒂
 4. ★清空全部
 5. →槽位0   6. →槽位1   7. →槽位2   8. →槽位3   9. →槽位4
_agents = ['流明', '琴柳', 'Lancet-2', '承曦格雷伊', '温蒂']   ← 无残留 Free
⚠ 被 _do_sort 抹掉的 = []                                      ← 无丢失
```

**改动**（`services/agent_swap_service.py` `_do_select()` 自由位分支，共 3 行语义）：

```python
# 已在座或本轮已选的干员不再补位                      ← 修复缺陷 A
if name not in self._agent_list or name in self._selected:
    continue
...
self._selected.append(name)                            # ← 修复缺陷 B
self._agents[self._agents.index("Free")] = name        # ← 修复缺陷 B
```

对齐旧实现 `base_schedule.py:2939 / :2942 / :2956`（`selected.extend(...)`、`agents[agents.index("Free")] = selected_name[0]`、`exists.extend(selected)`）。

**验收结果**：

| 项 | 结果 |
|---|---|
| 新增回归测试 | `arknights_mower/tests/agent_swap_free_fill_tests.py`（4 条） |
| 测试在修复前的代码上 | **4 条全部失败**，症状与预测完全一致（见下） |
| 测试在修复后的代码上 | 4/4 通过 |
| 全量单测 | **35/35 OK**（原 31 + 新增 4） |
| `python -m compileall scheduler` | exit=0 |
| `ruff check arknights_mower/scheduler` | **38**（与基线持平，未新增） |
| `ruff check agent_swap_service.py` | 6（与基线持平） |

修复前 4 条测试的失败信息（证明测试确有鉴别力，不是"永远通过"的空测试）：

```
AssertionError: '流明' unexpectedly found in ['流明','流明','流明'] : 流明 已在房内，不应被补位点选
AssertionError: 1 != 3 : 补位点到了重复干员：['流明', '流明', '流明']
AssertionError: 2 != 5 : 清空后应重建全部 5 个槽位，实际 ['SLOT0', 'SLOT1']
AssertionError: 3 != 0 : 仍有未占用的 Free：['流明', '琴柳', 'Free', 'Free', 'Free']
```

**尚未实机验证**：以上均为离线仿真 + 单测结论。**真机补位路径仍未跑过**（需要真实空闲干员存在才能观察到补位生效）。实机验证项见 A6。

---

#### 缺陷 B（本次新发现）：`_do_sort` 抹掉 free 补位结果 🔴 **已修（离线验证）**

**发现方式**：为验证"方案 B 是否够用"而做的离线仿真 —— 把 `流明/琴柳` 从候选里剔除，模拟"筛选已修好"的情形，结果发现补位结果仍会丢失。

**机制**（`services/agent_swap_service.py:246-266`）：

| 行 | 代码 | 后果 |
|---|---|---|
| 247 | `agents = [a for a in self._agents if a != "Free"]` | Free 位被丢弃，只剩定向干员 |
| 253 | `exists = self._selected` | `_selected` 只含定向干员 |
| 255-257 | `for a in agents: if a in exists` | `click_order` 只含定向干员的位置 |
| 258 | `tap(INFRA_CLEAR_ALL)` | **清空全部选中（含刚补位的干员）** |
| 260-262 | 只重新点击 `click_order` | 补位干员不会被补回 |

**根因**：修复前的 free 分支点了人之后**既不写 `_selected`，也不替换 `_agents` 里的 Free**，所以 `_do_sort` 天然看不见它。旧实现不会丢，因为它在 `:2939` 记录、`:2942` 替换 Free 位、`:2956` 一并纳入 `exists`。

**为何与缺陷 A 一并修**：两者是同一处改动 —— 补位命中后写入 `_selected` + 替换 Free 位后，`_do_sort` 自动包含补位干员并重新点击。若只修 A，自由位仍填不上（仿真已证明）。

---


### BUG-2 — 换人失败被报告为"成功" 🔴 **待修（但注意：非本次回归引入）**

**位置**：`scheduler/services/agent_swap_service.py:237-239`

翻满 `MAX_PAGE` 时，代码 `logger.error("AgentSwap: max page reached")` 后 **`return []`**（返回空步骤队列）而不是抛异常。于是 `run_steps()` 正常走完 → `dispatch.execute()` 返回 `True` → **`state.error=False`**。

**后果**：主循环认为任务成功，实际一个干员都没换。

⚠️ **更正**：回归前的版本在同样位置**也是 `return []`**（`3541ad09~1` 中 `max page reached` / `end of list` / `free end of list` 三处均 `return []`）。因此这是**既有设计缺陷，不是 v5 重构引入的回归**。仍需修，但不属于"修复回归"范畴。

**修法（待确认）**：应改为抛 `SchedulerError`（或返回失败信号），让 `state.error=True`。注意异常隔离（AGENTS.md 规则 3）的目的是"不让主循环崩溃"，而非"把失败伪装成成功"——`AbstractExecutor.safe_execute()` 捕获后写日志即可，无需吞掉失败语义。

---

### BUG-3 — `_last_names` 变成只写变量（保护缺失，但**不是** 50 次翻页的原因）

**位置**：`scheduler/services/agent_swap_service.py:114 / 216 / 252`

**症状**：`self._last_names` 现在**只有赋值、没有比较**：

| | `_last_names` 出现 |
|---|---|
| 当前 | `= None`（114）、`= None`（216）、`= None`（252）——**3 处全是赋值** |
| 回归前 | 2 处赋值 **+ 2 处 `if cur_names == self._last_names:` 比较** |

回归前的逻辑是：本页识别结果与上一页**完全相同** → 说明翻到底 → 立即停止。与 BUG-1 同源，都是 `3541ad09` 的 Step 重写删掉的。

---

#### ⚠️ 更正：我曾把 50 次翻页归因于此，**这是错的**

我原先写"这正是连续翻 50 页的直接原因"。用日志实测**推翻**了这个说法：

```
扫描页数           = 51
相邻页完全相同次数 = 0      <== _last_names 保护会触发 0 次
```

**相邻两页从未完全相同**（实测平均重叠仅 2~3 个干员）。所以即使这个保护存在，本次运行中它**一次都不会触发**，翻页照样会走满 50 页。我的归因是错的。

**真正的原因**（由用户观察指出，日志证实）：

| 事实 | 数据 |
|---|---|
| 本次 `filter` 全程 | `ALL`（**未筛选**） |
| 第 50 页累计覆盖 | `398/415` 个干员（95.9%） |
| 按此速率覆盖全部需要 | **约 52 页 > `MAX_PAGE=50`** |
| 第 50 页是否仍有新干员 | **是** → 列表未到底，被 `MAX_PAGE` 截断 |

即：**未筛选的总览表本身就比 `MAX_PAGE=50` 更长**，翻页走满是必然的，与 `_last_names` 无关。

**为什么 free 路径不加筛选是合理的**（用户观点，成立）：补空位只需"任意合法干员"，无需限定职业，所以不设 filter 是正确设计；`_get_target_filter()` 只在**指定目标干员时**才返回职业筛选。因此问题不在"该筛选却没筛选"，而在 `MAX_PAGE` 对未筛选列表偏小。

**BUG-1 修复后该路径已不再触发**：第 0 页就有 12 个合法候选人，补 3 个空位 = 3 次点击、**翻页 0 次**（见 BUG-1 的 A/B 验证）。

**性质修正**：
- `_last_names` 只写变量**仍是缺陷**（回归丢失），但在本次场景中**无实际影响**，其真实影响范围**未经实测**，不应夸大。
- 真正需要保留的语义是"翻页到列表末尾就停"，而"相邻页相同"只是它的一种近似判据 —— 对未筛选长列表并不灵敏。
- `MAX_PAGE=50` 与"未筛选总览表约 52 页"的关系，值得在 BUG-2 修法里一并考虑。

---

### 教训 — 识别读数 ≠ 游戏事实

本次验证过程中，`RoomReader` 曾在**同一画面**上连续两次读出不同结果：

```
dormitory_2 [流明(24) 琴柳(24) 空 空 傀影(0)]     ← 界面被轻微遮挡时
dormitory_2 [缄默德克萨斯(19) 空 空 空 空]         ← 下一次读取
```

一度被误判为"宿舍被改乱"。实际是 UI 遮挡导致识别不稳，换班本身是**成功**的。

**结论**：
1. 房间模板匹配分（如 `dormitory` 0.9997）**只能证明"看的是哪个房间"，不能证明"读到的干员是对的"** —— 两者必须分开验证。
2. 任何基于单次 `RoomReader` 读数的结论都不可靠。判断换人结果应**连读 2~3 次取一致值**，或直接以游戏内实际排班为准。
3. 验证脚本 `verify_shift_live.py` 应增加该重读逻辑，避免再次误导。
4. **教训的对象是分析者，不是代码**：看到异常读数应先怀疑识别本身，而不是先断言用户的游戏被破坏。

**附带确认**：`MainLoop` 接受空 planner 列表（`MainLoop(state, [], dispatch, infra)`），可用于受控单任务测试。

---

### A3 — 清理启动期的硬编码测试代码 ⬜ 待做

> ⚠️ **本节已并入 Session 分派计划 → 见 §10.3 「S1」**。本处保留背景与验收细节，**执行以 §10.3 为准**。

**要做什么**

| 项 | 位置 | 处理 |
|---|---|---|
| 硬编码假任务 | `bootstrap.py:135-140` | 删掉 `state.task_queue.push(SHIFT_ON room_1_1 ["Current","能天使","空爆"])` |
| 注释掉的 planner | `bootstrap.py:58, 61` | 明确 `InfraScanPlanner` 的去留，不留注释死代码 |
| `test-arrange` 入口 | `scheduler/__main__.py` | 明确其定位（临时调试 or 保留） |

⚠️ **行为变更警告**：删掉假任务后，启动**不再自动换 room_1_1 的班**。这是恢复正确行为，但会改变现有观察结果，需你确认。

**验收标准**

1. `grep -n "test:" arknights_mower/scheduler/bootstrap.py` 无输出
2. `grep -rn "InfraScanPlanner" arknights_mower/scheduler/bootstrap.py` 只剩有效代码或完全移除，无注释残留
3. `python -m py_compile` 通过
4. 启动后日志**不再出现** `test: pushed SHIFT_ON for room_1_1`
5. `ruff check arknights_mower/scheduler` 错误数 **≤ 38**（不新增）

---

### A4 — 删除死代码 ⬜ 待做

> ️ **本节已拆入 §10.3 「S1」（`agent_selection`/`registry`/`hooks`）与「S2」（`safe_execute`/`LegacyPlannerAdapter`）**。
> 执行以 §10.3 为准；`agent_selection.py` 的**去留已裁定为删除**（见 §10.4 第 6 项）。

**要做什么**

| 目标 | 位置 | 判断 |
|---|---|---|
| `scheduler/infra/registry.py` | 6 行 | 无引用 → 删 |
| `scheduler/hooks.py` | 2 行 | 无引用 → 删 |
| `AbstractExecutor.safe_execute()` | `executors/base.py:68` | 与 `dispatch.execute()` 的异常处理重复 → 删 |
| `LegacyPlannerAdapter` | `planners/base.py` | 无使用 → 删 |
| `AgentSelection` | `infra/agent_selection.py`（752 行） | 已接线（`bootstrap.py:43` 构造并存入 `InfraKit.agent_selector`），但**全代码库无人调用其 `run()`** → 决定去留 |
| `scheduler/REWRITE_PLAN.md` | 孤儿文档 | 移到 `.opencode/` |

**验收标准**

1. 每个删除目标执行 `grep -rn "<name>" arknights_mower/ --include=*.py` 均**无引用**（先证明再删）
2. 删除后 `import arknights_mower.scheduler.bootstrap` 成功
3. `python -m unittest discover -s arknights_mower/tests -p "*_tests.py"` → **35/35 通过**（当前基线；原文档写 31，已更新）
4. `ruff check arknights_mower/scheduler` 错误数**下降**（删除未使用 import 会自然减少）
5. `scripts/verify_scheduler_live.py` 仍能跑完只读探测

---

### A5 — 消除硬编码坐标/阈值 ⬜ 待做

> ⚠️ **本节已拆入 §10.3 「S2」（`Step` 下沉 + `base.py` 坐标 + `TRADE_ORDER_AGENTS` 预置）与「S4」（`navigator.py` 的 11 处尺寸）**。
> `room_reader.py` 的像素裁剪区归 **S3**。执行以 §10.3 为准。

**要做什么**（违反 AGENTS.md 规则 4）

| 位置 | 现状 | 改为 |
|---|---|---|
| `executors/base.py:94` | `tap(670/1920, 750/1080)` | `constants` 中的 `TapPosition` |
| `navigator.py` 行 101,106,109-112,115,118,119,234,301 | 手写 `1920` / `1080`（共 11 处） | `SCREEN_W` / `SCREEN_H` |
| `infra/room_reader.py:35-45` | 像素裁剪区硬编码 | 常量 |
| `planners/order.py:8` + `planners/exhaust.py:8` | `TRADE_ORDER_AGENTS` 重复定义 | 收敛到一处 |
| `Step`/`StepRetry`/`StepRestart` | 定义在 `executors/base.py` | 下沉到 `scheduler/steps.py` |

**验收标准**

1. `grep -rn "1920\|1080" arknights_mower/scheduler/ --include=*.py` 只应命中 `constants.py`（定义处）
2. `grep -rn "TRADE_ORDER_AGENTS" arknights_mower/scheduler/` 只有一处定义 + import 处
3. `python -m py_compile` 全部通过
4. 实机回归：`scripts/verify_shift_live.py --room room_1_1` 仍走完完整链路
5. `ruff check` 错误数 **≤ A4 后水平**

---

### A6 — 实机验证自由位补位 ⬜ 待做（A5 后可做，或提前单独做）

**为什么单独列出**：A+B 修复目前**只有离线仿真 + 单测**背书，真机补位路径从未跑通过。旧实现时代的日志里也没有"补位成功"的样本可比对。

**前提条件**：必须存在**真实空闲干员**（`current_room == ""`），否则补位无候选人，空位保持为空是**正确**行为，无法验证修复。

**要做什么**

1. 开局先只读探测：确认 `dormitory_2` 房内为 `流明/琴柳/空/空/空`，且至少存在 3 名空闲干员
2. 用受控脚本跑单房间补位：`scripts/verify_shift_live.py --room dormitory_2 --seconds 240`
3. 跑完立即只读复查 `dormitory_2` 的房内构成

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | 补位点选次数 | 日志 `free tap` 出现 **3 次**，且**互不相同**（不是同一个干员重复 3 次） |
| 2 | 不点已在房干员 | 日志**不得**出现 `free tap 流明` / `free tap 琴柳` |
| 3 | 最终房内构成 | `dormitory_2` 5 个位置**全部非空**，前两位仍是 `流明/琴柳` |
| 4 | 无丢失 | 清空全部（`_do_sort`）之后重新点击的槽位数 = 5，补位 3 人全部补回 |
| 5 | 目标干员未被挤出 | `流明` / `琴柳` 仍在房内（补位不得顶掉定向干员） |
| 6 | 无异常 | 日志无 `unhandled error` / `StepRetry` 死循环 |
| 7 | 翻页次数 | 补位应在**第 0 页**完成，`swipe page=N` 计数应为 **0**（BUG-1 修复后的预期） |

**回滚方式**：若补位结果错误，人工在游戏内调整该宿舍即可；本次改动只影响"自由位选谁"，不改变定向干员的安排逻辑。

---

## 四、阶段 B：统一 Planner 接口 ⬜ 待做

> ⚠️ **本节已并入 §10.3 「S6」**；阻塞点（多任务接口）**已裁定为 `plan_all` 纯函数式扩展**，见 §10.4 第 5 项。执行以 §10.3 为准。

**要做什么**

把 7 个不合规 planner 统一到 `condition(state)` / `make_task(state)`。

**必须先解决的阻塞点**

`ShiftPlanner.plan()` **一次推入多个任务**，而 `AbstractPlanner.plan()` 只返回**一个** `SchedulerTask`。这是接口不一致的根因，须先决定方案：

- 方案 1：`make_task` 返回 `list[SchedulerTask]`，`AbstractPlanner.plan()` 相应支持
- 方案 2：拆成多个单一职责 planner
- 方案 3：ShiftPlanner 内部维护待发队列，每次 `make_task` 吐一个

⚠️ 未经确认不要动手 —— 这会改变任务入队语义。

**验收标准**

1. `grep -rn "def plan(" arknights_mower/scheduler/planners/*.py` 只应有 `base.py` 的 1 处
2. 每个 planner 都实现 `condition()` + `make_task()`
3. `python -m unittest discover -s arknights_mower/tests -p "*_tests.py"` → 35/35
4. 实机：`scripts/verify_shift_live.py` 能跑完
5. 换班行为与阶段 A 实测结果**一致**（干员、路径、日志顺序）

---

## 五、阶段 C：清理与拆分 ⬜ 待做

> ⚠️ **本节已并入 §10.3**：C1→S1，C2→S1，C3→S2+S5+S6，C4→S3/S4/S5，C5→S5。执行以 §10.3 为准。

| 子阶段 | 内容 | 对应 Session | 验收标准 |
|---|---|---|---|
| **C1** | 判定 `infra/agent_selection.py`（752 行）去留 → **已裁定删除** | **S1** | grep 无引用 + 测试通过 |
| **C2** | 清空全部 `time.sleep(`（17 处） | **S1** | `grep -rn "time\.sleep(" arknights_mower/scheduler/` **无输出**（含 `agent_selection` 16 处、`__main__.py:22`）；且 `grep -rn "^import time" scheduler/` 无输出 |
| **C3** | 去重常量 | **S2** 预置 → **S5/S6** 接入 | 同一常量只有一处定义 |
| **C4** | 拆分 >300 行文件 | **S3**（619）/ **S4**（450）/ **S5**（572） | `scheduler/` 内**无** >300 行文件 |
| **C5** | 修 `state.py` 的 `self.plan` 回归 | **S5** | 见下方专节 |

### C5 — 已知缺陷：`state.py` 的 `self.plan` 回归  待修

> ⚠️ **本节已并入 §10.3 「S5」**，裁定为**现在修**（§10.4 第 7 项）。

**问题（有 v1 对照，属回归）**

| | v1 `utils/operators.py` | v5 `scheduler/state.py` |
|---|---|---|
| 调用 | line 145：`self.swap_plan([False] * len(self.backup_plans))` — **无条件** | line 45-46：`if self._backup_plans:` — **有条件** |
| `backup_plans=[]` 时 | `self.plan` 正常填充 | `self.plan` 恒为 `{}` |

**后果**：`backup_plans` 为空时，`dorm_names` 为空 → 报**误导性**错误 `宿舍优先级和当前宿舍不匹配`（真正原因是计划未加载）。

**验收标准**

1. `plan.json` 的 `backup_plans` 为 `[]` 时，构造 `SchedulerState` **不再**抛该 `ConfigError`
2. `backup_plans` 非空时行为与当前一致
3. 新增单测覆盖两种情形
4. `python -m unittest discover` → **35 以上**

---

## 六、阶段 D：官方 7 步路线图（宏观）

阶段 A–C 是**让实机可信**的前置工作。官方 7 步是**功能迁移**主线，对应关系如下：

| # | 官方步骤 | 现状 | 本手册对应 |
|---|---|---|---|
| 1 | Architecture Redesign | ✅ 骨架已就位，A1 修好断线 | 阶段 A |
| 2 | Graph / Navigation | ✅ 场景图可用（实机验证：能导航到基建任意页） | 阶段 A5 |
| 3 | Recognition 取繁从简 | ⬜ 未开始 | — |
| 4 | Business Logic Migration | 🔶 3/9 executor、3/10 planner | 阶段 B |
| 5 | Full Chain | 🔶 `MainLoop` 已能跑，planner 注册未全 | 阶段 A3 |
| 6 | Copilot | ⬜ 3 文件 `NotImplementedError` | — |
| 7 | Android | ⬜ 未开始 | — |

### 各步验收标准（官方口径 + 可测化）

| # | 验收标准 |
|---|---|
| 1 | 各模块可独立 import；`DevicePort` 可用；单测通过 |
| 2 | 场景图能导航到任意目标场景；实机走通 `INDEX ↔ INFRA_MAIN` |
| 3 | `recognize.py` 缩减到 ~500 行，仅保留 `template_matching`；180 处 `find()` 结果与旧版一致 |
| 4 | 9/9 executor 与 10/10 planner 迁移完毕；每个都跑通完整流程 |
| 5 | `MainLoop` + planners + executors + database 端到端联调通过 |
| 6 | JSON 战斗计划重放，指令序列与旧版一致 |
| 7 | Android 真机（MediaProjection + AccessibilityService）跑通基建 |

---

## 七、验证工具

> ⚠️ **本节路径已被 §10.2 取代**：测试统一迁到仓库根 `tests/`。S0 已完成迁移，
> 下表 `scripts/...` 路径**已作废**，一律改用右列的 `tests/live/...`。

| 脚本 | 用途 | 安全性 | S0 后路径 |
|---|---|---|---|
| `scripts/verify_scheduler_live.py` | 只读探测：连接 / 截图 / 识别 / 路径规划 | ✅ **不点击游戏** | `tests/live/verify_scheduler_live.py` |
| `scripts/verify_scheduler_live.py --run` | 构建全套 + 跑 `MainLoop` + 验证 pause/stop | ⚠️ 会构建真实设备链路 | 同上 |
| `scripts/verify_shift_live.py --room <room>` | 真实换班（单房间，墙钟上限兜底） | ⚠️ **会真的点击游戏** | `tests/live/verify_shift_live.py` |
| `scripts/probe_select_panel.py --room <room>` | 只读探测干员选择面板：复现 `_do_scan` 读数，判定 free 分支会点谁 | ⚠️ 只点 1 次空位开面板，**不点任何干员**，随后原路退出不提交 | `tests/live/probe_select_panel.py` |

> `scripts/fix_runtime_dlls.py` **留在 `scripts/`**（构建工具，非测试）。

**`probe_select_panel.py` 用法与保证**

```powershell
$env:PYTHONIOENCODING="utf-8"
.venv\Scripts\python.exe tests\live\probe_select_panel.py --room dormitory_2
```

1. 导航到 `INFRA_MAIN` → 进入目标房间
2. **只 tap 一次**「空位」打开选择面板（`INFRA_ROOM_SLOT_TAP`）
3. 用 `operator_list(full_scan=True)` 复现 `_do_scan` 的 cache，打印前若干条目
4. 判定 free 分支取到的第一个合法候选是谁，并标注计划内干员是否在列表中（=会被误点）
5. 连续 `back` 退回到 `INFRA_MAIN`，**不提交任何变更**

实测确认：该脚本只产生「打开面板」1 次点击，全程无干员点击，结束后 scene 回到 201。

**单测**

```powershell
$env:PYTHONIOENCODING="utf-8"
.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "*_tests.py"
```

> **S0 已交付**：旧的 `-s arknights_mower/tests` 写法**已作废**（该目录已删除），一律用上面这条。
> `-t .` 必需 —— 缺 `-t` 时顶层包会解析成 `unit` 而非 `tests`，同一批测试文件被加载成
> 两个模块对象（S0 实测），模块级状态与 `isinstance` 判定失准。

**Lint 基线**：`ruff check arknights_mower/scheduler` = 38 errors（基线，只减不增）。

---

## 八、待决策事项

> ✅ **全部事项已由主控裁定，见 §10.4。** 本表仅作历史索引，**不再更新**。

| # | 事项 | 影响 | 状态 |
|---|---|---|---|
| 1 | ~~A2 是否执行~~ | 真实换人已验证通过 | ✅ 已决 |
| 2 | **BUG-2/BUG-3 修法**：翻页到底后应抛异常还是返回失败信号 | 决定失败能否正确上报 | ✅ **已决：抛异常**（§10.4-2 → S3） |
| 3 | **BUG-1 修复后是否重跑 A2**（验证"补空位"选人路径） | 决定选人路径能否验证 | ✅ **已决：是**（§10.4-3 → S13） |
| 4 | A3 删除假任务后启动行为改变 | 需确认 | ✅ **已决：接受**（§10.4-4 → S1） |
| 5 | 阶段 B 的多任务接口方案（3 选 1） | 阻塞阶段 B | ✅ **已决：plan_all 扩展**（§10.4-5 → S6） |
| 6 | `agent_selection.py`（752 行）去留 | 影响 C1/C4 | ✅ **已决：删除**（§10.4-6 → S1） |
| 7 | C5 是否现在修 `state.py` 回归 | 影响 `backup_plans=[]` 的用户 | ✅ **已决：现在修**（§10.4-7 → S5） |
| 8 | 是否构建 `ui/dist`（Flask 才能提供网页界面） | 影响图形化验证 | ✅ **已决：不构建**（§10.4-8） |
| 9 | `_find_in_cache` / `_find_free_in_cache` 死方法：删还是接回 | 影响 BUG-1 修法风格 | ✅ **已决：删除**（§10.4-9 → S3） |
| 10 | **`MAX_PAGE=50` 是否偏小**（未筛选总览表实测约 52 页） | 决定"找目标干员"能否翻到列表末尾 | ✅ **已决：保持 50**，改“到末尾就停”判据（§10.4-10 → S3） |
| 11 | **验证脚本是否加"连读 2~3 次取一致值"** | 决定读数可信度 | ✅ **已决：加**，作为 tests/live/ 强制规范（§10.4-11） |
| 12 | ~~BUG-1 遗留问题 2 是否实机验证~~ | **已验证**：free 分支确实会点中已在房内的干员 | ✅ 已决 |
| 13 | ~~**free 分支候选过滤修法**~~ | **已决并已实现**：A+B 一并修 —— 候选排除 `self._selected`，命中后写入 `_selected` + 替换 `_agents` 中的 Free 名额。未接入 `state` / `free_blacklist`（留待 A6 观察后再定） | ✅ 已决 |
| 14 | **是否把 `SchedulerState` 传入 `AgentSwapService`**（以获得 `current_room == ""` 空闲判定 + `free_blacklist`） | 决定是否完全对齐旧实现语义；本次按"从简"未做 | ✅ **已决：传入**（§10.4-14 → S3） |

---

## 九、纪律（AGENTS.md 硬性要求摘要）

1. UI 操作用 scene-driven 状态机，或 `run_steps()` 队列
2. 每次操作只点一次（tap-once）
3. 任务异常必须隔离，不得让主循环崩溃
4. 零硬编码坐标/阈值（唯一例外：`recognize.py` 的 1920×1080 参考坐标）
5. **禁止 `time.sleep()`** —— 等待一律走 `PauseController.wait_if_paused()` / `wait_scene_stable()`
6. 新代码只依赖 `DevicePort`，不依赖 `utils/device/Device`
7. 一个 class 一个文件，文件 ≤ 300 行
8. `domain/` 只放数据类，业务逻辑移入 `services/`
9. 从简：不为"未来可能需要"预留
10. 先确认再动手

---

## 十、Session 分派总览（执行手册）

> 本章是**分发给其他 AI 的唯一依据**。每个 Session 独立开工、独立验收、独立提交。
> 主控（map）只维护本章与基线，不代替 Session 施工。

### 10.0 分派总览表

**规则：一个 Session 独占一组文件。同 Wave 内的 Session 文件集互不相交，可并行分发。**

| ID | 名称 | Wave | 依赖 | 独占文件（写权限） | 规模 |
|---|---|---|---|---|---|
| **S0** | 测试基建与统一门禁 | 0 | — | `tests/**`（新建）、`.github/workflows/*.yml` | 中 |
| **S1** | 启动期清理 + 死代码删除 | 1 | S0 | `bootstrap.py`、`scheduler/__main__.py`、`infra/__init__.py`、`errors.py`；**删除** `infra/agent_selection.py`、`infra/registry.py`、`hooks.py`、`REWRITE_PLAN.md` | 中 |
| **S2** | 基础层：`Step` 下沉 + 常量收敛 | 1 | S0 | 新建 `scheduler/steps.py`、`executors/base.py`、`planners/base.py`、`constants.py` | 小 |
| **S3** | 换班服务：BUG-2/3 + 拆分 619 行 | 1 | S0 | `services/agent_swap_service.py`、**`executors/shift.py`**、`infra/room_reader.py` + 新建 `services/agent_swap_*.py` | 大 |
| **S4** | 导航层拆分（450 → ≤300） | 1 | S0 | `navigator.py` + 新建 `navigator_*.py` | 中 |
| **S5** | 状态层拆分（572 → ≤300）+ C5 回归 | 1 | S0 | `state.py` + 新建 `state_*.py` | 中 |
| **S6** | Planner 统一接口 + 常量去重 + planner 接线 | 2 | S2, S5 | `planners/*.py`（含 `base.py`，见注）、`loop.py`、`bootstrap.py` 的 `_build_planners` | 大 |
| **S7** | Step 4：跑单（run_order） | 3 | S6 | `executors/run_order.py`、`planners/order.py` + 新建 service/domain | 大 |
| **S8** | Step 4：线索（clue） | 3 | S6 | `executors/clue.py`、`planners/clue.py` + 新建 | 大 |
| **S9** | Step 4：技能专精（skill） | 3 | S6 | `executors/skill.py`、`planners/skill.py` + 新建 | 中 |
| **S10** | Step 4：用尽下班（exhaust） | 3 | S6 | `executors/exhaust.py`、`planners/exhaust.py` + 新建 | 中 |
| **S11** | Step 4：肥鸭充能（fiammetta） | 3 | S6 | `executors/fiammetta.py`、`planners/fiammetta.py` + 新建 | 中 |
| **S12** | Step 4：纠错（correction） | 3 | S6 | `executors/correction.py` + 新建 | 中 |
| **S13** | **实机验收总集**（唯一允许连真机） | 4 | S1–S12 | `tests/live/**` | 大 |
| **S14** | 官方步骤 3：识别层收敛 | 5 | S13 | `utils/recognize/**`、`scheduler/scene.py` | 大 |
| **S15** | 官方步骤 5：Full Chain 端到端 | 5 | S13 | `dispatch.py`、`hooks` 接线（`loop.py` 见注） | 中 |
| **S16** | 官方步骤 6：Copilot | 6 | S15 | `scheduler/copilot/**` | 大 |
| **S17** | 官方步骤 7：Android | 7 | S16 | 打包与平台层 | 大 |

**Wave 依赖图**

> **注：跨 Wave 的文件复用不是冲突。** 独占规则只约束**同一 Wave 内并行**的 Session。
> Wave 是顺序执行的（Wave 2 开工时 Wave 1 已合并完毕），因此 `planners/base.py` 由 S2（Wave 1）建、S6（Wave 2）改，
> `loop.py` 由 S6（Wave 2）改、S15（Wave 5）再改 —— 均无冲突。表中"独占"= **该 Wave 内的排他写权限**。

```
S0 ──┬─> S1 ─
     ├─> S2 ──> S6 > S7..S12 ─> S13 ─┬─> S14
     ├─> S3 ─┤                          └─> S15 > S16 > S17
     ├─> S4 ┤
     └─> S5 ─┘
```

---

### 10.1 通用契约（每个 Session 开工前必读）

#### 每个 Session 必须交付的 4 件事

1. **代码变更**（仅限自己的独占文件集）
2. **离线测试**（放在 `tests/unit/`，命名 `*_tests.py`）
3. **门禁证据**（§10.1 的 5 条命令逐条贴出真实输出）
4. **回到本手册**：在本章 10.3 对应小节末尾追加一行 `结果：<日期> <状态> <新基线数字>` —— **只增不改他人条目**

#### 门禁命令（5 条，全部必跑）

```powershell
$env:PYTHONIOENCODING="utf-8"     # Windows 控制台中文必需，否则日志乱码
$py = ".venv\Scripts\python.exe"

# G1 单测（基线 35 passed, exit 0）
& $py -m unittest discover -s tests -t . -p "*_tests.py"

# G2 编译（基线 exit 0）
& $py -m compileall -q arknights_mower/scheduler

# G3 Lint（基线 38 errors；只减不增）
& $py -m ruff check arknights_mower/scheduler --output-format=concise

# G4 导入自检（基线 exit 0）
& $py -c "import arknights_mower.scheduler.bootstrap"

# G5 夹具元测试（基线 13 tests OK, exit 0）—— 不混入 G1 的业务基线
& $py -m unittest tests.harness.selftest
```

> **G5 是 S0 新增的门禁**：`tests/harness/selftest.py` 刻意**不**匹配 `*_tests.py`，因此**不进 G1**。
> 理由正当：夹具自身的变动（如新增一个 mock 方法）不应该污染 35 这个**业务**用例基线，
> 否则每个 Session 都要解释"为什么 35 变成了 36"。夹具的守卫（逐目录查 `__init__.py`、
> 查重复加载）单独跑：判据是 **13 passed**。G5 用例数随夹具增长，**只增不减**。

> ⚠️ **exit code 解读（主控已实测，勿误判）**
>
> | 门禁 | 基线 exit | 说明 |
> |---|---|---|
> | G1 | **0** | 必须为 0 |
> | G2 | **0** | 必须为 0 |
> | G3 | **1** | ⚠️ **基线就是 1** —— ruff 发现 38 个错误时必然返回 1。**判据是错误数 ≤38，不是 exit code** |
> | G4 | **0** | 注意会打印一行日志（`screenshot_cleanup`），不是错误 |
> | G5 | **0** | 必须为 0 |
>
> G3 的判据一律看输出末行的 **error 计数**，不要看 exit code。

**取错误数（报告里贴这个）**

```powershell
& $py -m ruff check arknights_mower/scheduler --output-format=concise 2>&1 |
    Select-String "^\S+:\d+:\d+:" | Measure-Object | Select-Object -ExpandProperty Count
```

**各 Session 的 ruff 预算（主控实测，逐个文件数出来的，勿自行改判据）**

全量 38 个错误按文件分布如下。**每个 Session 只对自己文件负责**。

| Session | 自己文件的错误数 | 收工后全量应 ≤ | 实测 | 说明 |
|---|---|---|---|---|
| （起点） | — | 38 | — | 基线 |
| S1 | **5**（`bootstrap.py` 3 + `agent_selection.py` 2） | **33** | ✅ **33** | 删 `agent_selection.py` 消 2 个 + 自清 3 个（含 F401） |
| S2 | **1**（`executors/base.py`） | **32** | — | 另 6 个在 `agent_swap_service.py`，**属 S3** |
| S3 | **8**（`agent_swap_service.py` 6 + `shift.py` 1 + `room_reader.py` 1） | **24** | — | 本 Wave 最大头 |
| S4 | **7**（全在 `navigator.py`） | **17** | — | 可清零 |
| S5 | **4**（全在 `state.py`） | **13** | — | 可清零 |

> ✅ **自洽校验**：串行走完 S1→S5 后剩 **13** 个错误，恰等于未分配文件的错误总和
> （`infra_scan.py` 4、`planners/shift.py` 2、`planners/order.py` 2，
> 以及 `queue.py`/`operator_service.py`/`dispatch.py`/`workshop.py`/`exhaust.py` 各 1 = 13）。
> 说明这份预算没有漏算也没有重复计算。

> ⚠️ **不要给 S1 设 "≤30" 这种目标** —— 主控实测其文件只占 5 个错误，**33 才是最佳可达值**。
> 若某 Session 报告"未达目标"，先核对它是否在为**别人的文件**背账。
> 上述残留 13 个由 **S6 及后续 Wave** 处理。

#### 通用验收硬指标

| # | 指标 | 判据 |
|---|---|---|
| 1 | G1 单测 | **≥ 35 passed 且 exit 0**，且**本 Session 新增测试全绿** |
| 2 | G2 编译 | exit 0 |
| 3 | G3 Lint | `scheduler` 错误数 **≤ 38**（不得新增）。各 Session 只对自己文件负责，逐 Wave 自然下降 |
| 4 | G4 导入 | exit 0 |
| 4b | G5 夹具元测试 | **≥ 13 passed 且 exit 0** |
| 5 | 文件行数 | **本 Session 触碰的每个文件 ≤ 300 行**（含新建文件、含新建的测试文件；见下方裁定） |
| 6 | 无新增违规 | 不新增 `time.sleep(`、不新增手写坐标/阈值、不新增 `NotImplementedError` |
| 7 | 向后兼容 | 不修改 `solvers/`、`utils/`（S14 除外） |
| 8 | 可独立提交 | 变更可单独成为一个 commit，不依赖同 Wave 其他 Session |

#### 绝对禁止（违反即打回）

- 修改**不属于自己独占文件集**的文件。需要他人文件配合时，**在该文件用一行机械 import 兼容**，或提请在手册中登记依赖，**不得直接改**
- 使用 `sandbox_permissions` 或申请提权
- 用 `Measure-Object -Line` 统计行数（**会漏算空行**，必须用 Python `len(read_text().splitlines())`）
- 把 300 行文件"拆"成两个都超 300 行的文件；拆出的新文件同样受 300 行约束
- 为了凑测试数量写无断言测试
- 删除任何**未先用 grep 证明零引用**的代码

#### 报告格式（固定模板）

```
## Session <ID> 报告
- 状态：完成 / 阻塞
- 变更文件：<列表 + 行数变化，如 navigator.py 450 → 287>
- 门禁：G1=<Ran N tests OK/exit> G2=<exit> G3=<N errors> G4=<exit>
- 新增测试：<文件:用例数>
- 基线数字变化：<如 scheduler 总行数 5678 → 5400；>300 行文件 4 → 3>
- 遗留/未做：<明确列出，不掩盖>
- 需要主控裁定：<有/无>
```

---

### 10.2 测试目录契约（S0 产出，全员遵守）

#### 唯一测试根目录：`tests/`（仓库根）

```
tests/
├── __init__.py                 # 必需（见下方"发现陷阱"）
├── _bootstrap.py               # 统一把仓库根插入 sys.path，供所有测试/live 脚本 import
├── harness/
│   ├── __init__.py
│   ├── mock_device.py          # MockDevicePort：记录 tap/swipe/screencap 调用序列
│   └── mock_recognizer.py      # MockRecognizer：按预设 scene 序列回放
├── fixtures/
│   ├── __init__.py
│   └── *.json                  # 实机探测回来的真实读数（只读，不得手编）
├── unit/                       # 离线单测（gate G1 只收集这里）
│   ├── __init__.py
│   ├── scheduler/              # 与 arknights_mower/scheduler 目录结构镜像
│   │   ── __init__.py
│   └── *_tests.py
└── live/                       # 实机脚本（不参与 G1 收集）
    ├── __init__.py
    ├── verify_scheduler_live.py
    ├── verify_shift_live.py
    └── probe_select_panel.py
```

#### ⚠️ 发现陷阱（已实测确认，S0 必须遵守）

`unittest discover` **不会递归进入没有 `__init__.py` 的子目录**。实测：

| 场景 | 结果 |
|---|---|
| `tests/unit/scheduler/alpha_tests.py`，**无** `__init__.py` | `Ran 1 test`（漏收子目录用例） |
| 同上，**有** `__init__.py`（逐层） | `Ran 2 tests` ✅ |

**因此**：`tests/` 及其**每一个子目录**都必须有 `__init__.py`，否则用例会被静默漏收 —— 这是最危险的失败模式（门禁"通过"但什么都没跑）。

> 现存 `arknights_mower/tests/` **没有** `__init__.py`，正是靠"所有文件平铺在一层"才侥幸被收集到。**S0 已迁移并补齐**（该目录已删除）。

#### 测试命名与写法规范

| 项 | 规范 |
|---|---|
| 文件名 | `*_tests.py`（与既有一致，**不要**用 `test_*.py`，否则与 G1 pattern 不匹配） |
| 类名 | `XxxTests(unittest.TestCase)` |
| 方法名 | `test_<行为>_<预期>`，中文可作注释但方法名用英文 |
| 框架 | `unittest`（**不引入 pytest**，保持零新增依赖） |
| 设备 | 一律用 `tests/harness/mock_device.py`，**禁止**在单测里构造真实 `Device` 或 `PCDevicePort` |
| 场景 | 用 `MockRecognizer` 回放 `Scene` 序列，禁止真实截图 |
| 网络/时间 | 禁止真实等待；需要时间用注入的 `now` 或假对象。**更禁止依赖墙钟的断言**（见下方硬要求 5） |
| 参考实现 | `tests/unit/agent_swap_free_fill_tests.py`（BUG-1 的 A/B 验证就是它做的，4 用例） |
| 结构 | 遵循 AGENTS.md **AAA**：Arrange / Act / Assert 三段，段间空行 |

#### 测试内容硬要求

1. **必须能证伪**：新测试要能在"故意改坏实现"时失败。提交前自己验一次（改坏 → 变红 → 改回）
2. **必须覆盖本 Session 的每一条验收标准**：验收标准有几条，至少几条断言（可合并用例）
3. **回归保护**：修复类 Session（S3/S5）必须为**每个修复项**留一个"曾经失败"的用例，并在用例 docstring 写明"修复前行为"
4. **不得为凑数**：无断言的用例、`assert True`、只 import 不校验的用例一律打回
5. **禁止墙钟依赖（禁止 flaky 用例）** —— 主控裁定，2026-09-18（因 S4 实际踩坑而加）

   **不许出现"结果取决于墙钟跑多快"的断言。** 典型错误形态：

   ```python
   # ❌ 错误：断言结果取决于 0.2 秒内能跑几轮循环
   result = nav.wait_scene_stable(max_duration=0.2, min_stable=2)
   self.assertFalse(result)      # 机器快 -> 轮次够多 -> 达到稳定 -> True -> 假红
   ```

   **规则**：

   - **反向（否定）用例必须"结构性不成立"，而不是"时间上来不及"。**
     要断言"永远不会稳定"，就让**每一对相邻帧都真的不同**（例如帧在两图之间无限循环），
     这样无论循环跑 1 轮还是 10 万轮，结果都恒为 `False`。
   - **不得靠"帧数多到跑不完"来制造否定用例。** `MockRecognizer.advance_frame()`
     会**钳制在最后一帧**（`min(cursor+1, len-1)`），所以帧序列用尽后相邻帧**会变得相同**，
     稳定必然达成 —— 能否在预算内跑完就成了竞态。
     ⚠️ 已知 `navigator_contract_tests.py` 的 `test_changing_frames_return_false_on_budget`
     用 400 帧绕过该钳制，代价是 **400 × 5.93 MiB ≈ 2.32 GiB 内存**。可用但不经济，
     新用例优先用"循环帧"而非"海量帧"。
   - **G1 必须连跑 ≥3 次**才算通过验收。单次 `OK` 不能证明不 flaky；主控与各 Session
     报告 G1 时，应贴出多次运行的结论（或至少说明已重复验证）。
   - 任何偶发失败（即使只出现一次）**一律按不通过处理**，不得以"重跑就好了"结案。

#### 实机脚本规范（`tests/live/`）

实机脚本**不属于离线门禁**，只在 S13 执行，但需遵守：

| 项 | 规范 |
|---|---|
| 墙体时钟上限 | 必须有 `--seconds` 参数（默认 ≤300），到点强制 `request_stop()` |
| 默认只读 | 默认行为**不得点击游戏**；会点击的必须要求显式 `--run` 或明确参数 |
| 读数可信度 | 关键判据（房内构成）必须**连读 2~3 次取一致值**（见 §四"识别读数 ≠ 游戏事实"教训） |
| 安全退出 | `finally` 里 `request_stop()`；`Ctrl+C` 可安全中断 |
| 日志 | 开头统一 `logger.setLevel(INFO)`（`utils/log.py:50` 硬编码为 DEBUG） |
| 路径引导 | 不得用 `parent.parent` 硬算仓库根，统一 `from tests._bootstrap import REPO_ROOT` |

#### `tests/live/` 的 `time.sleep` 规则（主控裁定，2026-09-17）

AGENTS.md 规则 6 禁止 `time.sleep()`，**该禁令对 `scheduler/` 生产代码是绝对的**：
`arknights_mower/scheduler/**` 内 `time.sleep(` 与 `import time` 必须恒为 **0** 命中（S1 已达成）。

**`tests/live/` 是唯一例外，但豁免范围极窄** —— 只允许**观察者线程**（通常是主线程）
使用 `time.sleep`，且必须同时满足：

| # | 条件 | 说明 |
|---|---|---|
| 1 | 只在**观察者线程** | 被观察的调度线程跑在 `threading.Thread` 里，其等待一律走 `pause.wait()` |
| 2 | **不得**用 `pause.wait()` 替代 | 观察者必须**独立于** pause 协议之外 —— 它要观测"暂停后是否真的停住了"。若观察者也调 `pause.wait()`，它会被同一个 pause 挂起，**验证本身失效** |
| 3 | 短暂且有界 | 单次 ≤2 秒，用于给对端线程留出推进时间，不得用于长等待 |
| 4 | 不得替代就绪判定 | 不得用固定 sleep 假装"界面已就绪"；那必须用 `wait_scene_stable()` |

**当前豁免清单（5 处，均为 S0 从 `scripts/` 迁入的既有脚本，已逐一核对用途）**：

| 位置 | 用途 | 裁定 |
|---|---|---|
| `verify_scheduler_live.py:87` | 采样循环里两帧之间留间隔 | ✅ 豁免。观察者采样节流，非就绪判定 |
| `verify_scheduler_live.py:143` | `pause.pause()` 前，给刚启动的 loop 线程留出进入运行态的时间 | ✅ 豁免 |
| `verify_scheduler_live.py:147` | **验证 `pause.pause()` 生效** | ✅ **必须保留** —— 正是条件 2 的典型场景 |
| `verify_scheduler_live.py:151` | **验证 `pause.resume()` 生效** | ✅ **必须保留** —— 同上 |
| `verify_shift_live.py:108` | 轮询任务队列状态与墙钟上限 | ✅ 豁免。观察者轮询 |

> ⚠️ **不要**以"清理 time.sleep"为名去改这 5 处。把它们换成 `pause.wait()` 会**破坏
> pause/stop 验证的有效性** —— 那不是修复，是把有效的测试改成永远通过的假测试。
> `probe_select_panel.py` 已是 0 处，无需处理。

---

### 10.3 各 Session 详细规约

#### S0 — 测试基建与统一门禁（Wave 0，**阻塞全员**）

**目标**：建立 `tests/` 唯一测试根，提供 mock 夹具，把现有 6 个遗留测试与 3 个实机脚本迁入，固化 4 条门禁命令。

**要做什么**

1. 按 §10.2 建目录，**每个子目录补 `__init__.py`**
2. 写 `tests/harness/mock_device.py`（`MockDevicePort`，实现 `DevicePort` 协议，记录调用序列）与 `mock_recognizer.py`
3. 迁移 `arknights_mower/tests/*_tests.py`（6 个）→ `tests/unit/`；**更新** `.github/workflows/format-check-and-test.yml:46` 与 `pyinstaller-win-shawn.yml:40` 的 `-s` 路径为 `tests`，并加 `-t .`
4. 迁移 `scripts/verify_scheduler_live.py`、`verify_shift_live.py`、`probe_select_panel.py` → `tests/live/`，改用 `tests._bootstrap.REPO_ROOT` 引导路径
5. 处理 `arknights_mower/tests/cuoyu.py`：非测试（导入即执行、标识符乱码、不被 `*_tests.py` 匹配）；**先 grep 证明零引用**，再删除
6. 写 `tests/README.md`：一页说清如何加用例、如何跑门禁

**初步引导**

- ⚠️ **门禁 G1 在 S0 完成前无法运行**：主控实测 `-s tests` 当前报 `ImportError: Start directory is not importable: 'tests'`。
  S0 期间**用旧命令自测**，S0 完成后再切新命令：
  ```powershell
  # S0 施工期间（旧路径）
  & $py -m unittest discover -s arknights_mower/tests -p "*_tests.py"
  ```
- `scripts/fix_runtime_dlls.py` **留在 `scripts/`**（构建工具，非测试）
- `DevicePort` 协议在 `scheduler/device_port.py`，先读它再写 mock
- 迁完后 G1 必须仍是 **35 passed**（迁移不得丢用例、不得因缺 `__init__.py` 漏收）
- 迁移用 `git mv` 保留历史，不要"新建 + 删除"

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | G1 = 35 passed exit 0 | 迁移动用了例数完全一致 |
| 2 | 子目录收集生效 | 在 `tests/unit/scheduler/` 放一个临时用例，G1 计数 **+1**（证明递归有效），验完删除 |
| 3 | 每个子目录都有 `__init__.py` | `Get-ChildItem tests -Recurse -Directory` 逐个检查 |
| 4 | G2/G3/G4 不劣化 | G3 ≤38 |
| 5 | CI 路径已更新 | 两个 workflow 文件 `-s tests -t .` |
| 6 | 实机脚本可只读跑通 | `tests/live/verify_scheduler_live.py`（无 `--run`）exit 0 |
| 7 | `tests/README.md` 存在 | 含门禁命令 |
| 8 | `cuoyu.py` 已删 | 报告含删除前 grep 输出（零匹配） |

**结果：2026-09-16 完成，主控已复验。** `tests/` 成为唯一测试根；旧 `arknights_mower/tests/` 已删除。
**主控复跑证据**：G1 `Ran 35 OK` exit 0 / G2 exit 0 / G3 38 errors / G4 exit 0 / **G5（S0 新增）`Ran 13 OK` exit 0**。
递归收集已由 S0 用 A–E 五步反证（删 `__init__.py` → 计数静默 35，无报错），逻辑正确。
`/arknights-mower/tests` 目录已确认不存在；`git rm`/`git mv` 历史保留。

> ️ **旧命令一律失效，全员改用新命令**（§10.1 的五条门禁命令即最终版本）：
>
> | 作废 | 改用 |
> |---|---|
> | `-s arknights_mower/tests` | **`-s tests -t .`** |
> | `scripts\verify_*.py` / `scripts\probe_select_panel.py` | **`tests\live\verify_*.py`** / **`tests\live\probe_select_panel.py`** |
>
> `-t .` **必需**：不加 `-t` 时顶层包会被解析成 `unit` 而非 `tests`，同一批测试文件
> 会同时以 `unit.xxx` 和 `tests.xxx` 两个模块名进入 `sys.modules`（实测 `is` 为 `False`），
> 模块级状态与 `isinstance` 判定失准、用例可能被重复执行。
> `scripts/fix_runtime_dlls.py` 留在 `scripts/`（构建工具，非测试）。
>
> 详细契约、夹具用法、递归收集陷阱证据见 `tests/README.md`。

---

#### S1 — 启动期清理 + 死代码删除（Wave 1）

**目标**：一次解决四项违规 —— 硬编码假任务、752 行超长文件、16 处 `time.sleep`、5 处死代码。

**独占文件**：`bootstrap.py`、`scheduler/__main__.py`、`infra/__init__.py`、`errors.py`
**删除**：`infra/agent_selection.py`(752)、`infra/registry.py`(4)、`hooks.py`(2)、`scheduler/REWRITE_PLAN.md`

**主控裁定（已决，勿再讨论）**：`agent_selection.py` **删除**。理由：其 `run()` 全库零调用；功能已被实机验证过的 `services/agent_swap_service.py` 取代；删它同时消掉 `time.sleep` 违规（16 处）与超长文件违规。回退触发条件：若删除后发现任何实机链路依赖它 → 停止并上报（**预期不会**）。

**要做什么**

| 项 | 位置 | 处理 |
|---|---|---|
| 硬编码假任务 | `bootstrap.py:134-141` | 删除 `state.task_queue.push(SHIFT_ON room_1_1 [...])` 与 `logger.info("test: pushed...")` |
| 注释掉的 planner | `bootstrap.py:58,61` | 移除注释死代码（planner 接线交给 S6） |
| `test-arrange` 入口 | `scheduler/__main__.py:22` | 去掉 `import time` + `time.sleep(5)`，改用 `PauseController.wait()`；明确该入口定位并写入 docstring |
| `agent_selector` 字段 | `infra/__init__.py:15` | 从 `InfraKit` 移除 |
| `AgentSelection` 接线 | `bootstrap.py:31,43,50` | 删除构造与传参 |
| `AgentSelectionError` | `errors.py:24` | 随 `agent_selection.py` 一起删（删后 grep 确认零引用） |
| `InfraRegistry` | `infra/registry.py` | 删（零引用） |
| `LifecycleHook` | `hooks.py` | 删（零引用） |
| `safe_execute` | `executors/base.py:68` | ⚠️ **不归本 Session** —— 该文件属 S2，移交 S2 处理 |
| `REWRITE_PLAN.md` | `scheduler/` | 移到 `.opencode/` |

**初步引导**

- **先证明再删**：每个删除目标跑 `grep -rn "<name>" arknights_mower/ --include=*.py`，把输出贴进报告
- 已实测的引用事实（主控已核）：`safe_execute`=1(自身)、`LegacyPlannerAdapter`=1(自身)、`InfraRegistry`=1(自身)、`LifecycleHook`=1(自身)、`_find_in_cache`/`_find_free_in_cache`=各1(自身)、`agent_selector`=3(全在 bootstrap/`infra/__init__`)、`AgentSelection`=12(除 bootstrap 外全在自身)
- `__main__.py` 的 `time.sleep(5)` 语义是"等界面就绪" → 正确替代是 `pause.wait(5)` 或 `navigator.wait_scene_stable()`，**不是**删掉等待

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | 死代码零引用证明 | 报告含每个目标的 grep 输出 |
| 2 | `scheduler/` 无 `time.sleep(` | `grep -rn "time\.sleep(" arknights_mower/scheduler/` **无输出** |
| 3 | `scheduler/` 无 `^import time` | `grep -rn "^import time" arknights_mower/scheduler/` 无输出 |
| 4 | 超长文件 4 → **3** | 无 752 行文件 |
| 5 | G1 | ≥35 passed exit 0 |
| 6 | G2/G4 | exit 0 |
| 7 | G3 | **≤ 33 errors**（主控实测：你的文件只占 5 个错误，38 − 5 = 33 是最佳可达值。**不要**为他人文件的错误背账） |
| 8 | 启动无假任务 | 日志**不再出现** `test: pushed SHIFT_ON for room_1_1` |
| 9 | 新增测试 | 至少 1 个：断言 `InfraKit` 无 `agent_selector` 字段 + `bootstrap` 可导入 |
| 10 | G5 | ≥13 passed exit 0 |

**行为变更警告**：删除假任务后，启动**不再自动换 `room_1_1` 的班**。这是恢复正确行为，但会改变现有观察结果 —— 主控已确认接受。

**结果：2026-09-16 完成。** G1 `Ran 47 OK` exit 0 ｜ G2 exit 0 ｜ G3 **33 errors** ｜ G4 exit 0 ｜ G5 `Ran 13 OK` exit 0。
`scheduler/` 文件数 64 → **61**，总行数 5678 → **4928**，>300 行文件 **4 → 3**（752 行 `agent_selection.py` 已删，次大 619）。
已删 `infra/agent_selection.py`、`infra/registry.py`、`scheduler/hooks.py`、`errors.AgentSelectionError`；`InfraKit.agent_selector` 字段移除；启动期假任务与注释 planner 清除；`REWRITE_PLAN.md` 移至 `.opencode/`。
`time.sleep(` 与行首 `import time` 在 `scheduler/` 内**零命中**。S1 自己的 4 个文件 ruff 错误数 **0**（优于预算的 5，故全量 33 低于 §10.1 的 ≤33 预算上限）。
新增 `tests/unit/scheduler/bootstrap_cleanup_tests.py`（**12 用例**），已用 3 组证伪（加回字段/加回假任务/加回 time.sleep）验证可失败。

**主控复核补充（同日）**：门禁 5 项逐字复现通过，判定 **S1 通过**。另修正了 S1 测试中一条
**过宽断言** —— 原 `assertNotIn("InfraScanPlanner", source)` 会在 **S6 合法注册该 planner** 时误报
（主控已实测复现 `FAILED`）；已替换为两条意图精确的用例（查"注释掉的接线"而非"符号是否存在"）。
用例数 12 → **14**，且已验证：注入 S6 式合法接线后保持全绿，注入注释接线则变红。
详见 §10.4「S1 复核后新增裁定」。

---

#### S2 — 基础层：`Step` 下沉 + 常量收敛（Wave 1）

**目标**：把 `Step`/`StepRetry`/`StepRestart` 从 `executors/base.py` 下沉到 `scheduler/steps.py`；收敛硬编码坐标；为下游 Session 预置共享常量。

**独占文件**：新建 `scheduler/steps.py`、`executors/base.py`、`planners/base.py`、`constants.py`

**关键设计（必须照做，否则引起跨 Session 冲突）**

`executors/base.py` 保留**兼容再导出**，使既有 import 方**一行都不用改**：

```python
# executors/base.py 顶部
from arknights_mower.scheduler.steps import (  # noqa: F401
    Step,
    StepRestart,
    StepRetry,
)
```

理由：`Step` 被 `executors/infra_scan.py`、`executors/shift.py`、`services/agent_swap_service.py` 引用（后者是 `services/ → executors/` 的**层级倒挂**）。直接改三处 import 会撞上 S3/S6 的独占文件。用再导出即**零冲突**，倒挂可留待后续 Session 各自修。

**要做什么**

| 项 | 位置 | 处理 |
|---|---|---|
| `Step` 三等件下沉 | 新建 `steps.py` | 移入；`base.py` 再导出（见上） |
| `executors/base.py:94` 硬编码 | `tap(670/1920, 750/1080)` | 改引 `constants` 中的 `TapPosition` |
| `TRADE_ORDER_AGENTS` | `constants.py` | **新增一处定义**（下游 S5/S6 各自改为 import，本 Session 不动他们的文件） |
| `safe_execute` | `executors/base.py:68` | **删除**（零引用；与 `dispatch.execute()` 的异常隔离重复） |
| `LegacyPlannerAdapter` | `planners/base.py:41` | **删除**（零引用） |
| `MAX_PAGE` | `constants.py` | 若尚未定义则新增（S3 需要）；数值裁定见 §10.4 |
| `AbstractPlanner` 接口 | `planners/base.py` | **本 Session 只删死代码**；多任务接口改造属 S6，勿提前动 |

**初步引导**

- `constants.py` **只增不改名**：改名会波及 13 个 import 方（含 S3/S4/S5 的独占文件）
- `recognize.py` 的 1920×1080 参考坐标是 AGENTS.md 规则 4 的**唯一例外**，不要动
- `safe_execute` 删除后，确认 `AbstractExecutor` 仍能正常 `run_steps`（G1 覆盖）

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | `Step` 定义唯一 | `grep -rn "class Step" arknights_mower/scheduler/` **只有 `steps.py` 一处** |
| 2 | 兼容再导出生效 | `python -c "from arknights_mower.scheduler.executors.base import Step, StepRetry, StepRestart"` exit 0 |
| 3 | 三个原 import 方**未改也能跑** | 用 `git stash`/`git diff HEAD -- <file>` 证明**本 Session 的 diff 未触碰** `executors/infra_scan.py`、`executors/shift.py`、`services/agent_swap_service.py` |
| 4 | 死代码已删 | `grep -rn "safe_execute\|LegacyPlannerAdapter" arknights_mower/ --include=*.py` 无输出 |
| 5 | 无裸坐标 | `executors/base.py` 内无 `/1920` `/1080` 字面量 |
| 6 | `TRADE_ORDER_AGENTS` 已就位 | `constants.py` 有定义；G4 exit 0 |
| 7 | 行数 | `steps.py`、`base.py`、`planners/base.py`、`constants.py` 均 ≤300 |
| 8 | G1/G2/G4/G5 | G1 **≥36 passed 且全绿**（基线 35 + 本 Session 新增）、G2 exit 0、G4 exit 0、G5 ≥13 passed |
| 9 | G3 | **≤32**（你的文件占 1 个错误） |

> ⚠️ **标准 3 的判据必须按"本 Session 自己的 diff"来判，不能按"文件当前内容"判。**
> 主控已核对：**S3 拥有 `executors/shift.py`**（裁定 14 要求注入 `SchedulerState`）。
> 若串行下 S3 尚未开工，S2 看到的是原始的 `shift.py`；但**判据仍应按自己的 diff 写**，
> 这样无论串行还是并行都成立。
> 正确做法：收工时 `git diff --stat` 只应包含 `steps.py` / `executors/base.py` /
> `planners/base.py` / `constants.py` 四个文件。

**结果：2026-09-17 完成。** G1 `Ran 74 OK` exit 0 ｜ G2 exit 0 ｜ G3 **32 errors** ｜ G4 exit 0 ｜ G5 `Ran 13 OK` exit 0。
`scheduler/` 文件数 61 → **62**（新增 `steps.py`），总行数 4928 → **4954**，>300 行文件 **3 → 3**（619 / 572 / 450，均属 S3/S5/S4）。
★ **`TRADE_ORDER_AGENTS` 已就位**：`constants.py` 唯一权威定义 `["但书", "龙舌兰", "佩佩", "可露希尔"]`，S5/S6 可直接 import（本次未动下游 5 处散落定义）。
`Step`/`StepRetry`/`StepRestart` 定义唯一落在 `steps.py`；`executors/base.py` 兼容再导出生效，三个既有引用方**一行未改**（`git diff --name-only` 对 `infra_scan.py`/`shift.py`/`agent_swap_service.py` 为空）。
已删死代码 `safe_execute`（`dispatch.TaskDispatch.execute` 已承担异常隔离）与 `LegacyPlannerAdapter`，全库零命中。
`670/1920, 750/1080` 收敛为 `TapPosition.LEAVE_INFRASTRUCTURE`；`executors/base.py` 内无 `/1920` `/1080` 字面量。
S2 自己的 4 个文件 ruff 错误数 **0**（预算 1，优于预算，故全量 32 低于 ≤32 上限）。
新增 `tests/unit/scheduler/steps_tests.py`（10 例）+ `run_steps_tests.py`（15 例）= **25 用例**，全部走 `MockDevicePort` + `MockRecognizer`，零真实等待。
已用 6 组证伪验证可失败；其中证伪 №2 暴露了"逐字 grep 等价"断言可被 `670 / 1920`（空格）绕过，已补一条 AST 级守卫。

---

#### S3 — 换班服务：BUG-2/BUG-3 + 拆分 619 行（Wave 1）

**目标**：修掉"翻页到底静默 `return []`"这一**失败无法上报**的缺陷；把 619 行 `agent_swap_service.py` 拆到 ≤300 行。

**独占文件**：`services/agent_swap_service.py`、**`executors/shift.py`**、`infra/room_reader.py` + 新建 `services/agent_swap_*.py`

> **为什么含 `executors/shift.py`**（主控已核对）：`AgentSwapService` 的**唯一构造点**在
> `executors/shift.py:46`（`ShiftExecutor.swap_service` 属性内）。裁定 14 要求注入 `SchedulerState`，
> 必须改这个构造点 —— 否则该裁定无法落地。`shift.py` 只有 166 行，未超限。
> **与 S2 不冲突**：S2 依赖"`executors/base.py` 再导出 `Step`"，因此 S2 **明令不得改** `shift.py`；
> 本 Session 拥有它。两者交集为空。

**主控裁定**

| 事项 | 裁定 | 理由 |
|---|---|---|
| BUG-2 翻页到底 | **抛专用异常 `AgentSwapError`（继承 `Exception`），不得静默 `return []`；⚠️ 也**不得**抛 `StepRetry`** | 静默返回使 `Dispatch` 判定为"成功"，`state.error=False`，错误被吞。**而 `StepRetry` 会造成无限循环** —— 见下方主控实测 |
| **BUG-2 ↔ BUG-3 是同一处代码** | **必须一并修** | 重构前 `_do_select` 里是 `if cur_names == self._last_names: ... return []`。「翻到底检测」（BUG-3 丢失的比较）与「静默返回」（BUG-2）**本就是同一条语句**。只修 BUG-2 而不恢复 BUG-3，就**没有"翻到底"的判据**，只剩 `MAX_PAGE` 兜底 |
| `MAX_PAGE=50` | **保持 50**，改为"到末尾就停"的判据 | 实测未筛选总览表约 52 页；单纯调大只是把问题推后。BUG-1 修复后第 0 页即可补位，翻页非关键路径 |
| 事项 14：注入 `SchedulerState` | **注入** | 需要 `current_room == ""` 空闲判定与 `free_blacklist` 才能对齐旧实现语义；"从简"在此处导致**语义不正确**，不算精简 |
| `_find_in_cache`/`_find_free_in_cache` | **删除** | 各自零引用；补位已由 `_selected` + Free 位替换实现。⚠️ **`_find` 不删**（有 8 处调用，见下方） |

**⚠️ 主控实测：`StepRetry` 会死循环（这是本 Session 最容易踩的坑）**

`AgentSwapService._run_steps`（`agent_swap_service.py:127-153`）与 `AbstractExecutor.run_steps`
**不同**：它**没有** `guard()`、**没有**超时兜底。其异常处理是：

| 抛出 | 结果 |
|---|---|
| `StepRetry` | L146 `continue` → **队列不推进**（`popleft` 未执行）→ `queue[0]` 仍是同一个 `select` step → 再次 `_do_select` → 同样条件 → 再抛 → **无限循环，且无超时能救** ❌ |
| `StepRestart` | L148 重置队列 → 重跑 → 同样条件 → **同样无限循环** ❌ |
| **普通异常** | L150 `except Exception` → `return False` → `run()` 返回 `False` → `shift.py:130` `raise StepRestart` → 外层 `AbstractExecutor.run_steps` 有 `guard()`（`_timeout=10min`）兜底 → `dispatch` 捕获 → `loop.py:91 state.error = True` ✅ |

**故必须新增 `AgentSwapError(Exception)` 并抛它**（详见 §10.4「S3 复核前新增裁定」）。

**要做什么**

1. **修 BUG-3（恢复末尾检测）+ BUG-2（改为抛异常）—— 同一处代码，一并做**：
   在 `_do_select` 里恢复重构前的比较逻辑：
   ```python
   cur_names = [c[0] for c in self._cache]
   if cur_names == self._last_names:      # 本页与上页完全相同 → 翻到底
       logger.error("AgentSwap: reached end of list ...")
       raise AgentSwapError(...)          # 不是 return []
   self._last_names = cur_names
   ```
   同时 `MAX_PAGE` 命中处（`:242-244`）也改为抛 `AgentSwapError`，
   **并在日志保留 `max page reached` 字样**（S13 会 grep 它）。
   ⚠️ **`_do_prepare:170` 与 `_do_sort:271` 的 `return []` 是"正常完成"路径，不得改**
   （`return []` 表示追加空队列 → 循环结束 → 返回 `True`）。
2. 按职责拆分（建议）：`agent_swap_service.py`（编排）+ `agent_swap_scan.py`（扫描/翻页/末尾检测）+ `agent_swap_filter.py`（筛选/排序）+ `agent_swap_arrange.py`（选人/落位）
3. 注入 `SchedulerState`（构造参数），接入空闲判定与 `free_blacklist`
4. 删两个死方法
5. 为 BUG-2、BUG-3、空闲判定各补离线用例

**初步引导**

- **BUG-1 已修，勿回退**：`_do_scan` 必须写 `self._cache`（**不是**局部变量），且补位命中后要写 `_selected` + 替换 `_agents` 中的 `Free`（缺陷 A+B 一体修复，勿只修一半）
- **⚠️ BUG-3 尚未修复 —— 它是你的活**：当前 `_last_names` 在 `:114 / :216 / :257` **3 处全是赋值、0 处比较**（主控实测）。此前 S3 规约误写"BUG-3 已修，勿回退"，**已更正**
- **⚠️ 空闲判定的方向不要搞反**（主控核对 legacy `get_free_list` 后更正）：
  legacy 的 `v.current_room == ""` 是**入选**条件 —— 空闲（不在任何房间）才算自由人。
  正确语义：**排除** `current_room != ""`（已在别处工作）的干员；
  `current_room == ""` 的才是合法候选。当前实现用 `name not in self._agent_list`
  做筛选（`_agent_list` 是**全体干员名集合**，条件几乎恒 False，等于没筛）——
  这正是裁定 14 要修的语义缺失
- 还要一并排除 `state.config.free_blacklist` 里的干员
- `tests/unit/` 里已有 `agent_swap_free_fill_tests.py` 4 个用例作为回归网，拆分后必须仍全绿
- 拆分**不得改变**行为：拆分前后用同一 mock 场景对比 tap/swipe 序列（这是最好的拆分验收手段）。
  **建议用 Mixin（每个文件一个类、按职责切分）**，因为它零行为变化且天然满足
  "一个 class 一个文件"；若用组合则需显式传状态，风险更高
- 注意 `services/ → executors/` 的层级倒挂：**S2 已完成**，`scheduler/steps.py` 已存在，
  本 Session 应把 `:34` 的 import 改为 `from arknights_mower.scheduler.steps import ...`

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | 所有文件 ≤300 行 | 含新建文件 |
| 2 | BUG-2 已修 | 新用例：构造"翻页到 `MAX_PAGE` 仍无候选"场景，断言**抛出 `AgentSwapError`** 而非返回空 |
| 2b | **BUG-3 已修** | 新用例：连续两页 `_cache` 内容完全相同 → 断言**立即抛 `AgentSwapError`**（恢复比较逻辑）；且 `_last_names` **有比较点**（非只写变量） |
| 3 | 行为不变 | 拆分前后 mock 场景的 tap/swipe 调用序列**完全一致**（报告贴对比） |
| 4 | BUG-1 回归网仍绿 | 原 4 个 free-fill 用例全过 |
| 5 | **空闲判定生效** | 新用例：**`current_room != ""`（已在别处工作）的干员被排除出候选**；`current_room == ""` 的**才是**合法候选；`free_blacklist` 内的被排除。⚠️ 方向不可反 —— 见 §10.4 的更正 |
| 6 | 死方法已删 | grep `_find_in_cache` / `_find_free_in_cache` 无输出。⚠️ `_find` **必须保留**（8 处调用） |
| 7 | 超长文件 → **2** | 无 619 行文件 |
| 8 | 构造点已改 | `executors/shift.py` 传入 `state`；G4 exit 0 |
| 9 | G1/G2/G4/G5 | G1 **≥74 passed 且全绿**（不得丢用例）、G2 exit 0、G4 exit 0、G5 ≥13 passed |
| 10 | G3 | **≤24**（你的文件占 8 个错误） |

**结果：2026-09-17 完成。** G1 `Ran 97 OK` exit 0 ｜ G2 exit 0 ｜ G3 **24 errors** ｜ G4 exit 0 ｜ G5 `Ran 13 OK` exit 0。
`scheduler/` 文件数 62 → **69**（新增 7 个 `agent_swap_*.py`），总行数 4954 → **5315**，>300 行文件 **3 → 2**（619 已消除，剩 `state.py` 572 / `navigator.py` 450，属 S5/S4）。
`agent_swap_service.py` 619 → **113**；新建 `agent_swap_errors/base/scan/filter/select/arrange/train.py`（41/156/108/99/116/135/195，均 ≤300）。
★ **BUG-2 与 BUG-3 已一并修**（§10.4 缺陷 1）：`agent_swap_scan.py` 恢复 `if cur_names == self._last_names` 比较，命中即抛 `AgentSwapError`；`MAX_PAGE` 分支同样抛异常并**保留 `max page reached` 日志字样**。`_last_names` 比较点由 AST 级用例守卫（防再次退化为只写变量）。**未使用 `StepRetry`/`StepRestart`**（§10.4 缺陷 2 的无限循环），异常经 `_run_steps` 的 `except Exception` → `return False` → `shift.py` 抛 `StepRestart` → 外层 `AbstractExecutor.run_steps` 的 10 分钟 `guard()` 兜底 → `dispatch` → `loop.py:91 state.error = True`。
★ **空闲判定方向已按 §10.4 缺陷 3 更正**：`current_room == ""`（空闲）是**入选**条件；排除 `current_room != ""`（已在别处工作）与 `config.free_blacklist`。`SchedulerState` 经 `shift.py:swap_service` 注入（`state=self.infra.state`，链路 `bootstrap.py:43 → InfraKit.state → shift.py → AgentSwapService`）；`state=None` 时保持拆分前行为，故 BUG-1 回归网 4 用例仍全绿。
已删死方法 `_find_in_cache` / `_find_free_in_cache`（零真实引用）；**`_find` 保留**（1 处定义 + 8 处调用，AST 用例守卫）；`services/ → executors/` 倒挂消除（`Step` 改自 `steps.py`）。
**行为保持证据**：在 HEAD 建临时 worktree 取拆分前快照，与拆分后逐项 diff —— 4 条正常路径（free 补位 / 定向+切换 / 真实筛选原语 / 训练位）tap·swipe·screencap 序列**逐字相同**；差异**仅**出现在两条错误路径（`end_of_list` 与 `max_page`：`returned:True` → `returned:False`，即修复本身）。
新增 `tests/unit/scheduler/` 三文件共 **23 用例**（`agent_swap_error_reporting_tests` 7 + `agent_swap_free_candidate_tests` 7 + `agent_swap_split_guard_tests` 9），全部走 `MockDevicePort` + `MockRecognizer`，零真实等待。
已用 **6 组证伪**验证可失败（去比较 / 翻到底静默 / MAX_PAGE 静默 / 空闲方向反转 / 不筛黑名单 / 误删 `_find`），每组「注入→红→还原→绿」。
本 Session 10 个文件 ruff 错误 **8 → 0**（全量 32 → 24，恰合预算）。

---

#### S4 — 导航层拆分（Wave 1）

**目标**：`navigator.py` 450 → ≤300，并消除 11 处手写 `1920`/`1080`。

**独占文件**：`navigator.py` + 新建 `navigator_*.py`

**要做什么**

1. 按行号把硬编码屏幕尺寸改为 `SCREEN_W`/`SCREEN_H`（实测位置：101,106,109-112,115,118,119,234,301）
2. 拆分建议（**已由主控更正，见下**）：`navigator.py`（核心：`__init__`/`navigate`/`enter_room`/检测/等待/点击原语，保留 L1-270）
   + `navigator_actions.py`（`_action_*` Mixin，**必须是 Mixin，不可用组合**）
3. `wait_scene_stable()` 是 AGENTS.md 规则 6 钦定的"等界面稳定"手段，**不得**改成 sleep

#### S4 分派前发现的三处规格缺陷（主控实测更正，2026-09-17）

**缺陷 1：`_action_*` 是 `getattr` 字符串分派，用组合拆分会让 55 个动作全部失效**

`navigate` 在 `navigator.py:62` 这样取处理器：

```python
handler = getattr(self, f"_action_{transition.action}", None)
if handler is None:
    logger.error(f"no handler for transition.action")
    return False
```

实测 `_action_*` 共 **55 个**。它们**必须留在 `self` 上** ——
若按"组合"把它们放进另一个对象（如 `self._actions = NavigatorActions(...)`），
`getattr(self, "_action_xxx")` 会**全部返回 `None`**，55 个动作同时报
`no handler` → **导航全面失效**。

**裁定：必须用 Mixin（或继承）拆分，`_action_*` 仍绑定在 `Navigator` 实例上。**
（S3 的 `agent_swap_*` 拆分同样用 Mixin，可直接参考它的写法。）

**缺陷 2：验收标准 6 的 grep 判据不可能通过**

原标准 6 写 `grep -n "time\.sleep\|import time" navigator*.py` **无输出**。
但实测 `navigator.py:178` **确实有** `import time as _time`，用于
`wait_scene_stable` 的**超时计时**：

```python
t0 = _time.time()
while _time.time() - t0 < max_duration:   # 计时预算，不是等待
```

`time.sleep(` **零命中**（正确），但 `import time` 会命中 →
**该判据无论怎么做都无法通过**。AGENTS.md 规则 6 禁止的是
`import time` **+ `time.sleep()`** 的组合，计时用途不在禁止之列。

**已更正标准 6 为：`time.sleep(` 零命中；并显式许可 `_time.time()` 用于超时预算。**

**缺陷 3：拆分建议里的 `navigator_graph.py` 没有代码可放**

原建议写 `navigator_graph.py`（路径搜索）。但实测 `navigator.py` **不含路径搜索代码** ——
它只在 `navigate:57` **调用** `self._graph.find_path(current, target)`，
而 `find_path` 定义在 **`graph.py:194`**（另一个文件，非 S4 独占）。
**该文件是空目标，已从建议中移除。**

**更正后的拆分面（主控实测行数）**

| 目标文件 | 内容 | 实测行数 |
|---|---|---|
| `navigator.py` | L1-270：`__init__`(13) / `navigate`(46) / `enter_room`(38) / `_detect_room`(30) / `_detect_digit`(11) / `wait_scene*`(37) / `_wait_room_detail`(22) / 点击与返回原语 | **~270** |
| `navigator_actions.py` | `_action_*` **Mixin**（55 个方法，L271-450，**连续尾部块**） | **~67** |

> ️ `_action_*` 是**连续的尾部块**（L271-450），无穿插的其他方法，
> 因此 Mixin 拆分非常干净。但核心文件 ~270 行**贴近 300 上限**，
> 若加注释/import 后超限，可再抽 `navigator_wait.py`（`wait_scene*` 共 37 行）。

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | **本 Session 负责的文件**无裸尺寸 | `navigator.py` 及新建文件内无 `1920`/`1080` 字面量（改用 `SCREEN_W`/`SCREEN_H`） |
| 2 | 所有文件 ≤300 行 | 无 450 行文件 |
| 3 | 导航行为不变 | 新用例：mock 场景下 `navigate(target)` 的路径序列与拆分前一致 |
| 3b | **55 个动作仍可分派** | 新用例：`getattr(nav, f"_action_{name}")` 对全部 55 个 action 名**均可取到**（防组合化回归） |
| 4 | `wait_scene_stable` 仍可用 | 有对应用例（连续帧相同即返回） |
| 5 | `Navigator` 签名未变 | `bootstrap.py:39` 与 `__main__.py:48` **均未被改**；构造参数一致 |
| 6 | **未引入 sleep** | `time.sleep(` **零命中**。✅ 显式许可 `import time as _time` + `_time.time()` 用于**超时预算**（`wait_scene_stable`），非等待 |
| 7 | G1/G2/G4/G5 | G1 **≥97 passed 且全绿**（不得丢用例）、G2 exit 0、G4 exit 0、G5 ≥13 passed |
| 8 | G3 | **≤17**（你的文件占 7 个错误：`navigator.py` 1:1 I001 / 3:21 F401 `Path` / 125:9 I001 / 126:25 F401 `numpy` / 154:9 I001 / 176:9 I001 / 205:9 I001） |

> ⚠️ **不要写"全 `scheduler/` 只剩 `constants.py` 命中"这种判据** —— 主控实测该目标**不可能由 S4 单独达成**：
> 另有 `copilot/tile.py:10,14`、`executors/base.py:94`（S2）、`executors/shift.py:164,166`（S3）命中。
> 全局清零是 **S14（识别层收敛）** 的收口目标，各 Session 只负责自己的文件。

**结果：2026-09-17 完成。** G1 `Ran 121 OK` exit 0 ｜ G2 exit 0 ｜ G3 **17 errors** ｜ G4 exit 0 ｜ G5 `Ran 13 OK` exit 0。
`scheduler/` 文件数 69 → **70**（新增 1 个 `navigator_actions.py`），总行数 5315 → **5355**，>300 行文件 **2 → 1**（450 已消除，仅剩 `state.py` 572，属 S5）。
`navigator.py` 450 → **288**；新建 `navigator_actions.py` **202**（均 ≤300）。
★ **用 Mixin 拆分（§10.4 缺陷 1）**：`class Navigator(NavigatorActionsMixin)`，55 个 `_action_*` 仍绑定在 `Navigator` 实例上；`navigate` 的 `getattr(self, f"_action_{...}")` **原样保留**，未改成分派表。行为级守卫 `test_all_graph_actions_resolve_on_instance` + `test_every_action_produces_device_activity` 对 `graph.py` 全部 55 个 action 名逐一断言可取到且调用后**确有设备动作**。
★ **11 处裸尺寸已消除**（实测位置 101,106,109-112,115,118,119,234,301）→ `SCREEN_W`/`SCREEN_H`（`constants.py` 既有常量，**未新增**）；AST 级守卫（非 grep，防 S2 的"空格绕过"假绿）。
★ **未引入 sleep（§10.4 缺陷 2）**：`time.sleep(` 零命中；`import time as _time` + `_time.time()` 的超时预算**原样保留**（未为"让 grep 干净"做无意义改写）。
★ **未创建 `navigator_graph.py`（§10.4 缺陷 3）**：`navigator.py` 不含路径搜索实现，未改 `graph.py`。
**行为保持证据**：拆分前（450 行版本）与拆分后各跑 14 个 mock 场景快照（`navigate` 路径 / tap / back / swipe / `wait_scene_stable` / `wait_room_detail` / 55 动作逐个调用轨迹），**逐项 diff 全部 SAME**；另用 AST 比对 70 个方法体的签名与结构，**仅** 5 个方法的 1920/1080 → 常量差异（`_action_announcement`/`_tap`/`enter_room`/`_detect_room`/`wait_scene_stable`）。
新增 `tests/unit/scheduler/` 三文件（`navigator_behaviour_tests.py` 11 用例 + `navigator_contract_tests.py` 13 用例 + 共享夹具 `navigator_scenarios.py`），全部走 `MockDevicePort` + `MockRecognizer`，零真实等待。
已用 **7 组证伪**验证可失败（组合化拆分 / 裸尺寸回归 / `wait_scene_stable` 判据 / 分派前缀改名 / 行数超限 / 引入 `time.sleep(` / 构造签名漂移），每组「注入→红→清 `__pycache__`→还原→绿」。
本 Session 2 个生产文件 ruff 错误 **7 → 0**（全量 24 → 17，恰合预算）；`bootstrap.py` 与 `__main__.py` **未被改动**（`git diff --name-only` 为空），构造签名逐参数一致。

补交结果：2026-09-18 完成 flaky 修复。单用例 30/30、单模块 10/10、全量 G1 连跑 6 次全绿。
★ **阻断项**：`test_crop_limits_compared_region` 的否定对照依赖**墙钟**（10 帧 + `MockRecognizer` 钳制 + 0.2s 预算；`advance_frame()` 钳制在最后一帧使相邻帧趋同，`True` 当且仅当 11 轮，而 0.2s 只够 7~10 轮）。主控复现 20 次单跑失败 1 次、全量 8 次失败 4 次；本 Session 独立复现 20 次单跑失败 1 次、40 次内捕获 `AssertionError: True is not false`，并独立证实"轮次 ≥11 才 True"（0.2s 实测 7/8/10 轮，2.0s 实测 11 轮）。
★ **修法（§10.2 硬要求 5）**：新增 `CyclingRecognizer`（放在本 Session 的 `navigator_scenarios.py`，**未改** S0 共享夹具 `tests/harness/mock_recognizer.py`）—— `advance_frame()` 改为 `(cursor+1) % len(frames)` 无限循环，两帧只在 `y >= 200`（裁剪区之外）不同，**每一对相邻帧都真的不同**（含首尾相接那对）。否定对照改用**宽裕**预算 `max_duration=2.0`：让结果为 `False` 的是**结构**而非"时间不够"。实测 `max_duration` = 0.05/0.1/0.2/0.5/1.0/2.0 下 `cropped` 恒 `True`（恒 3 轮）、`uncropped` 恒 `False` —— **不再依赖墙钟**。
★ **顺带降内存**（可选顶）：`test_changing_frames_return_false_on_budget`（400 帧 ≈ **2.32 GiB** 峰值）改用 `CyclingRecognizer` 2 帧（≈ **11.87 MiB**，降 **200×**）并更名 `test_always_changing_frames_return_false`，语义不变（帧恒变 → 永不稳定），预算同样放宽到 2.0s。
★ **可证伪**：① 删掉 `crop` 传参 → `assertTrue(cropped)` 红（`AssertionError: False is not true`）；② 改回"10 帧 + 钳制 + 宽裕预算" → `assertFalse(uncropped)` 红（`AssertionError: True is not false`）—— **宽裕预算正是最强回归守卫**：它使旧 flaky 写法**确定性**失败，而非偶发。两条均「注入→红→清 `__pycache__`→还原→绿」。
**生产代码零改动**：`git diff --name-only -- arknights_mower/` 为空；`tests/harness/**` 无改动。用例总数仍 **121**（更名 1 个 + 改写 2 个，未增删），ruff 仍 **17**。

---

#### S5 — 状态层拆分 + C5 回归（Wave 1）

**目标**：`state.py` 572 → ≤300；修 `self.plan` 回归。

**独占文件**：`state.py` + 新建 `state_*.py`

**要做什么**

1. **C5 修复**：`state.py:45-46` 的 `if self._backup_plans:` 改为**无条件** `self.swap_plan([False] * len(self.backup_plans))`（对齐 v1 `utils/operators.py:145`）
2. **拆分（已由主控更正，见下方缺陷 1）**：把 5 个初始化方法（330 行）移入
   `state_init*.py`，`state.py` 保留对外 API + 属性；**必须再拆一刀**使每个文件 ≤300
3. `TRADE_ORDER_AGENTS`：`state.py:16` 的本地副本改为从 `constants` import（`constants.py` 由 S2 预置）
4. **`domain/` 纪律**：拆出的若是数据类，业务逻辑必须落 `services/`；`need_to_refresh`、`not_valid` 一律不得留在数据类里

> ⚠️ **`predict_exhaust` / `available_free` / `not_valid` 的迁移边界（主控已核对调用方，
> 2026-09-18 更正：`available_free` 原判为"无外部调用"是错的）**
>
> | 方法 | scheduler 内调用方 | 处置 |
> |---|---|---|
> | `predict_exhaust` | **`planners/shift.py:30`（S6，Wave 2）** | **必须保留 `SchedulerState.predict_exhaust` 可调用** —— 可为薄委托，**不得删名**。物理实现可移入 `services/operator_service.py` |
> | `available_free` | **`planners/exhaust.py:25`（S6，Wave 2）** ⚠️ 原规约漏了 | **同样必须保留可调用且签名不变**（`available_free(free_type: str = "high")`，调用方同时用默认值与 `"low"`） |
> | `not_valid` | 无（scheduler 内） | 可自由迁移 |
>
> 即：`domain/` 纪律要求"业务逻辑移出数据类"，但**对外方法名是两个 S6 planner 的契约**。
> 二者用"薄委托"同时满足。
>
> ⚠️ `solvers/base_schedule.py` 里的 `available_free()` / `not_valid()` 调用属**旧世界的另一个类**
> （`op_data`），与 `SchedulerState` **无关**，不要当成调用方。

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | C5 已修 | 新用例：`backup_plans=[]` 时构造 `SchedulerState`，断言 `state.plan` **非空**且 `config` 非 `None` |
| 2 | 非空时行为不变 | 新用例：`backup_plans` 非空时 `plan` 与修复前一致 |
| 3 | 常量去重（本文件） | `state.py` 无 `_TRADE_ORDER_AGENTS` 字面量定义，改为 import |
| 4 | `predict_exhaust` **与** `available_free` 仍可调用 | `python -c "from arknights_mower.scheduler.state import SchedulerState as S; assert hasattr(S,'predict_exhaust'); assert hasattr(S,'available_free')"` exit 0；且 `available_free` **签名含 `free_type` 参数**（S6 的 `planners/exhaust.py:25` 用 `available_free("low")`） |
| 5 | 所有文件 ≤300 行 | 无 572 行文件；**Wave 1 全部收工后全项目应 0 个超长文件** |
| 6 | G1/G2/G4/G5 | G1 **≥121 passed 且全绿**（不得丢用例；**须连跑 ≥3 次**，见 §10.2 硬要求 5）、G2 exit 0、G4 exit 0、G5 ≥13 passed |
| 7 | G3 | **≤13**（你的文件占 4 个错误；剩余的 13 个全属他人文件，**不是你的失败**） |
| 8 | `state_init*.py` 不超限 | 若照搬 330 行初始化逻辑会超 300 —— 必须再拆（见 §10.4 缺陷 1） |

> ️ **C5 测试怎么写（主控已实测出可行配方，2026-09-18）**
>
> **不要**直接 `SchedulerState(global_plan={"default_plan": ..., "backup_plans": []})` ——
> 主控实测该写法会**抛 `ConfigError`**，原因是 `_init_and_validate` 会读**真实**
> `config.conf.dorm_order`（`state.py:421-435`），与合成 plan 不匹配：
>
> ```
> ConfigError: 宿舍优先级和当前宿舍不匹配，请清除优先级自动排序或者自己更正
> ```
>
> 且该分支还会调 `config.save_conf()`（**写用户的 `conf.yml`**）——
> **单测绝不能触发它**（违反"禁止真实副作用"）。
>
> **可行配方**（主控已验证：修复前 RED / 修复后 GREEN / 非空对照不变）：
>
> 1. 用**子类**把校验隔离掉，只测 C5 本身：
>    ```python
>    class _Probe(SchedulerState):
>        def _init_and_validate(self, update: bool = False):
>            return None          # 隔离校验，避免读真实 config / 写 conf.yml
>    ```
> 2. `global_plan` 用**合成 Plan**（`Plan`/`PlanConfig`/`Room` 都是 dataclass，
>    无需 DB、无需设备）；干员名必须取自 `arknights_mower.data.agent_list`，
>    且**替换组非空**（`state.py:397` 要求）
> 3. 断言：`self.assertTrue(state.plan)` + `self.assertIsNotNone(state.config)`
>
> **证伪性已由主控确认**：
>
> | 场景 | 修复前 | 修复后 |
> |---|---|---|
> | `backup_plans=[]` | `plan={}` / `config=None` → **断言失败** ✅ | `plan` 非空 / `config` 已设 → **通过** |
> | `backup_plans` 非空（对照） | `plan` 非空 / `config` 已设 | **完全一致**（`plan_condition` 也一致） |
>
> 即：标准 1、2 可用**同一个测试文件的两个用例**覆盖，且都真能证伪。
>
> **注意 `PlanConfig` 的必填字段**（无默认值，构造时都要给）：
> `rest_in_full` / `exhaust_require` / `resting_priority` / `workaholic` /
> `free_blacklist` / `refresh_trading_config` / `refresh_drained` / `ope_resting_priority`
> （`ling_xi`、`resting_threshold`、`free_room` 有默认值）。

> **⚠️ 基线同步（主控，2026-09-17）**：本小节的 G1 下限原写 `≥35`，
> 那是 **Wave 0 开工前**的旧基线。S3 收工后实测 G1 = **97**，故更正为 **`≥97`**。
> ruff 预算 `≤13` 不变（S4 收工后预计 `17 − 4 = 13`，见 §10.4 的 S4 更正）。
>
> **⚠️ 再次同步（主控，2026-09-18）**：S4 收工后实测 **G1 = 121**，故本小节下限
> **以 `≥121` 为准**。ruff 预算 `≤13` 不变（`17 − 4 = 13` 实测成立，见下）。

#### S5 分派前发现的两处规格缺陷（主控实测更正，2026-09-18）

主控在写 S5 提示词前通读 `state.py`（572 行）全文，发现**两处会误导执行者**。

**缺陷 1：拆分建议指向的两个文件都是"空目标"**

原建议写 `state_plan.py`（`swap_plan`/`plan_condition`）+ `state_queue.py`（任务队列操作）。
主控逐项核对后确认**两者都不是实际的大块**：

| 建议目标 | 实测 | 问题 |
|---|---|---|
| `plan_condition` | **是属性**（L42 `self.plan_condition: list[bool] = []`、L201 赋值），**不是方法** | 无方法可搬 |
| `swap_plan` | 仅 **13 行**（L190-202） | 搬走也减不了几行 |
| 任务队列操作 | 仅 `save_tasks`(11 行) + `restore_tasks`(12 行) = **23 行**，且是 `TaskQueue` 的薄包装 | 收益极小 |

**`state.py` 真正的大块全在初始化逻辑**（主控实测行数）：

| 方法 | 行数 | `self.*` 属性数 |
|---|---|---|
| `_init_and_validate` | **133** | 15 |
| `_from_old_plan` | **77** | 1 |
| `_init_mood_limit` | **47** | 3 |
| `_add_operator` | **40** | 8 |
| `_build_dormitories` | **33** | 2 |
| **合计** | **330** | |

**更正后的拆分面**：把这 5 个初始化方法整体移入 **`state_init.py`**
→ `state.py` 约 **242 行**，`state_init.py` 约 **330 行**。

> ⚠️ **`state_init.py` 会超 300 行**，必须**再拆一刀**。建议：
> `state_init.py`（`_add_operator` + `_build_dormitories` + `_init_mood_limit`，约 120 行）
> + `state_plan_load.py`（`_from_old_plan` + `_init_and_validate` 的转换部分，约 210 行）。
> **具体切法由 S5 自定，但每个文件必须 ≤300 行。**
> 若用 Mixin，注意 §10.5 的"同一 Wave 内文件独占"仍成立（`state_*` 全是 S5 的）。

**缺陷 2：`available_free` 有 Wave 2 外部调用方（原规约漏了）**

原规约的迁移边界表写 `available_free` "**无外部调用**（仅 `state.py:267` 定义）→ 可自由迁移"。
**主控实测这是错的** —— `available_free` 与 `predict_exhaust` **同样有跨 Wave 调用方**：

```
available_free     planners/exhaust.py:25
                   current_resting = len(self._state.dormitories)
                                     - self._state.available_free()
                                     - self._state.available_free("low")
predict_exhaust    planners/shift.py:30        ← 原规约已正确识别
```

`planners/exhaust.py` 属 **S6（Wave 2）**，S5 不能改它。且该方法**有参数**
（`available_free(free_type: str = "high")`），调用方同时用默认值与 `"low"`。

> **裁定：`available_free` 与 `predict_exhaust` 同等对待 —— 必须保留
> `SchedulerState.available_free(...)` 可调用且签名不变（可为薄委托），不得删名。**
> 原规约"可自由迁移"三字会导致 S6 的 `exhaust.py` 在 Wave 2 直接 `AttributeError`。

（`not_valid` 确实无 scheduler 内调用方 —— 这条原规约正确，可自由迁移。
注意 `solvers/base_schedule.py` 里的 `available_free`/`not_valid` 调用属**旧世界另一个类**，
与 `SchedulerState` 无关，**不要**把它们当成调用方。）

结果：2026-09-18 完成。G1 127 OK（连跑 3 次全绿）、G3 13 errors，超长文件 0，ruff 13。

---

#### S6 — Planner 统一接口 + 常量去重 + 接线（Wave 2）

**目标**：7 个不合规 planner 统一到 `condition(state)`/`make_task(state)`；完成 planner 注册。

**独占文件**：`planners/*.py`（**含 `base.py`** —— S2 已在 Wave 1 收工，本 Session 可改）、`loop.py`（同上）、`bootstrap.py` 的 `_build_planners`

**主控裁定 —— 多任务接口（阻塞点已决）**

采用**方案 3 的变体**：`AbstractPlanner` 增加**可选**多任务钩子，`ShiftPlanner` 用它；`make_task` 保持返回**单个** `SchedulerTask`（不动既有 3 个合规 planner）。

```python
# planners/base.py
def plan(self, state) -> Optional[SchedulerTask]: ...          # 不变
def plan_all(self, state) -> list[SchedulerTask]:              # 新增，默认委托 plan()
    task = self.plan(state)
    return [task] if task is not None else []
```

`MainLoop._run_planners`（`loop.py`）改调 `plan_all`。

> 理由：方案 1（`make_task` 返回 list）会改动 3 个已合规 planner 与 `AbstractPlanner` 契约；方案 3 原样会在 `ShiftPlanner` 内藏状态、难以测试。`plan_all` 是**纯函数式**扩展，默认行为与现状**完全等价**，风险最低。

**要做什么**

| planner | 现状 | 改为 |
|---|---|---|
| `shift.py` | `plan(self)` 推多任务 | `condition` + `plan_all` |
| `exhaust.py` | `plan(self)` | `condition`+`make_task` |
| `fiammetta.py` | `plan(self, task_time)` | `condition`+`make_task` |
| `backup.py` | `plan(self, timing_value=999)` 返回 `bool` | `condition`+`make_task`（返回 task 或 None） |
| `order.py` | `plan_room`/`plan_all` | `condition`+`make_task`（多房间 → `plan_all`） |
| `clue.py`/`skill.py` | 骨架 | 骨架**保留**（S8/S9 迁移），但须继承合规签名 |
| `constants` 去重 | `order.py:8`、`exhaust.py:8` | 改为从 `constants` import（S2 已预置） |
| planner 接线 | `bootstrap.py:54-63` | 注册全部已迁移 planner，**移除注释死代码** |

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | `plan(` 定义唯一 | `grep -rn "def plan(" arknights_mower/scheduler/planners/*.py` 只有 `base.py` 的 1 处（`plan_all` 不算） |
| 2 | 全部实现 `condition`+`make_task` | 10 个 planner 逐个检查 |
| 3 | 常量单一定义 | `grep -rn "TRADE_ORDER_AGENTS" arknights_mower/scheduler/` 只有 `constants.py` 定义 + import 处 |
| 4 | 入队语义不变 | 新用例：`ShiftPlanner` 在同一天气/心情输入下产生的任务**集合**（含时间、类型、plan）与改造前一致 |
| 5 | 接线完整 | `bootstrap._build_planners` 返回全部已迁移 planner，无注释残留 |
| 6 | G1/G2/G3/G4 | 达标 |

---

#### S7–S12 — Step 4 业务迁移（Wave 3，六路并行）

**共同目标**：把 6 个骨架 executor + 对应 planner 从 `solvers/base_schedule.py` 迁入 `scheduler/`。

**共同独占文件规则**：每个 Session 只拥有 `executors/<X>.py`、`planners/<X>.py`，以及**自己新建**的 `services/<X>_*.py`、`domain/<X>*.py`。**六个 Session 之间不得互相修改文件**；共享逻辑需要下沉时，登记依赖由主控分配。

| Session | 执行器 | 规划器 | 旧实现来源（`solvers/base_schedule.py`） |
|---|---|---|---|
| **S7** | `run_order.py` | `order.py` | `plan_run_order` 1386、`run_order_solver` 1406、`get_order_remaining_time` 3146、`refresh_run_order_time` 3216、`accept_order` 3399 |
| **S8** | `clue.py` | `clue.py` | `clue_new` 1937、`overtake_room` 210 |
| **S9** | `skill.py` | `skill.py` | `skill_upgrade` 1227、`refresh_skill_time` 930、`choose_train` 2562、`choose_train_ope` 2639 |
| **S10** | `exhaust.py` | `exhaust.py` | `drone` 2323、`get_order` 2515、`rearrange_resting_priority` 1763 |
| **S11** | `fiammetta.py` | `fiammetta.py` | `plan_fia` 356、`check_fia` 1880、`initialize_operators` 1875 |
| **S12** | `correction.py` | — | `handle_error` 331、`todo_list` 1914、`skip` 3521 |

**共同要求**

1. **架构必须 scene-driven**：唯一模式 `while True: scene = get_scene(); if scene == A: ...`，参照 `solvers/base_schedule.py:generate_product()`（967）与已迁移的 `executors/shift.py`
2. 纯线性流程可用 `run_steps()` + `Step(name, enter, act, start)`
3. **tap-once**：单轮只点一次，重试交给外层 scene 循环
4. 异常隔离：`StepRetry`（原地重试）/ `StepRestart`（整队重来）；不得让主循环崩溃
5. **零硬编码**：坐标/阈值全部进 `constants.py`（**注意 `constants.py` 属 S2**，需要新增常量时在报告登记，由主控统一追加；不得自行修改）
6. 等待一律 `pause.wait_if_paused()` / `navigator.wait_scene_stable()`，**禁止 `time.sleep()`**
7. 新代码只依赖 `DevicePort`，不得 import `utils/device/Device`
8. 一个 class 一个文件，每个文件 ≤300 行
9. **不改旧代码业务逻辑**：`solvers/` 原方法**保持原样**（渐进迁移），只在新世界重写

**共同验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | 无骨架残留 | 本 Session 的两个文件内 `grep NotImplementedError` 无输出 |
| 2 | scene-driven | 代码中无 `State enum + method dispatch` 模式；有 `get_scene()` 驱动的循环 |
| 3 | 无 `time.sleep` | `grep -rn "time\.sleep(" <本 Session 文件>` 无输出 |
| 4 | 无裸坐标 | 文件内无 `1920`/`1080`/裸浮点坐标字面量 |
| 5 | 离线用例 | mock 场景下走通**主链路**：进入 → 操作 → 结束，断言 tap 序列与终态 |
| 6 | 异常隔离 | 用例：中途抛异常时 executor **不崩溃**、返回失败信号 |
| 7 | 文件 ≤300 行 | — |
| 8 | G1/G2/G3/G4 | 达标 |

**共同风险**：`solvers/base_schedule.py` 是 4,474 行上帝类，迁一个方法常牵出多个辅助方法。**只迁该 TaskType 真正需要的最小集合**（AGENTS.md 核心原则 1「从简」），其余留待后续，不得顺手全迁。

---

#### S13 — 实机验收总集（Wave 4，**唯一允许连真机**）

**目标**：把 Wave 1–3 的**全部离线结论**在真机上收口。这是唯一做真机验证的 Session。

**独占文件**：`tests/live/**`

**前置**：MuMu Player 12 已启动、游戏已登录、`adb 127.0.0.1:16416` 已连接、`maa_adb_path: D:\MuMuPlayer\nx_main\adb.exe`、`touch_method: scrcpy`。

**要验收的项（逐条独立、各自可回滚）**

| # | 验收项 | 判据 | 回滚 |
|---|---|---|---|
| 1 | 启动无假任务（S1） | 日志无 `test: pushed SHIFT_ON` | 无需回滚 |
| 2 | 导航全链路（S4） | 首页 → 基建 → 任意房间，scene 序列正确 | — |
| 3 | **自由位补位（A6/BUG-1）** | `free tap` 出现 **3 次且互不相同**；`swipe page` 计数 **0**；终点 5 位全非空、前两位仍是 `流明/琴柳` | 游戏内手工调宿舍 |
| 4 | BUG-2 失败可上报（S3） | 构造翻页到底场景，`state.error=True`，**不再静默成功** | — |
| 5 | 唯一真实换人（S3 拆分后） | 拆分前后换人结果一致（`dormitory_2` 减少人数路径） | 游戏内手工调宿舍 |
| 6 | C5 回归（S5） | `backup_plans=[]` 配置下启动**不再报** `宿舍优先级和当前宿舍不匹配` | — |
| 7 | 六个新 executor（S7–S12） | 逐个单任务跑通，各自 scene 链路正确 | 逐任务跳过 |
| 8 | 读数可信度 | 关键判据**连读 2~3 次取一致值**后才下结论 | — |
| 9 | **pause/stop 验证有效性（新增）** | `verify_scheduler_live.py` 的 pause/resume/stop 三项**真的验证到了**：pause 后 `is_paused=True` 且 loop 线程**不再推进**；resume 后恢复推进；`request_stop()` 后线程在 ≤10s 内结束。**若这三项变成恒真，即为无效验证** | — |

**必须遵守**

1. 每项开始前**先只读探测**，再决定是否执行点击
2. 全部脚本必须有 `--seconds` 墙钟上限（默认 ≤300）与 `finally: request_stop()`
3. **读数 ≠ 游戏事实**：`RoomReader` 曾同屏读出两个不同结果，一次读数不得作为结论
4. 每项结束立即只读复查终态
5. 失败项**不得**为了"通过"而放宽判据 —— 记录为失败并上报主控
6. **`tests/live/` 的 5 处 `time.sleep` 不得清理**（见 §10.2 的豁免规则）。其中
   `verify_scheduler_live.py:147,151` 是 pause/resume 验证的**必要**部分：观察者若改用
   `pause.wait()` 会被同一个 pause 挂起，导致验证恒真。清理它们会**破坏验收项 9**。

**验收标准**

| # | 标准 | 判据 |
|---|---|---|
| 1 | 8 项逐条有结论 | 报告含每项的实机日志片段 |
| 2 | 失败项有归因 | 明确指出是代码问题还是识别问题 |
| 3 | 无游戏数据破坏 | 除预期的换班/补位外，无其他房间被改动 |
| 4 | 脚本入 `tests/live/` | 且 README 说明每项的安全边界 |
| 5 | 离线门禁仍绿 | G1–G4 达标 |

---

#### S14–S17 — 后续（Wave 5–7，宏观）

**S14 官方步骤 3：识别层收敛**（`utils/recognize/**`、`scheduler/scene.py`）

- `get_scene()` 仍是 ~80 分支 `if/elif` 链，而 `scene.py` 的 `SCENE_RULES` **已定义但从未被使用** → 改为数据驱动的规则表
- `__init__.py` 仍有 2 处 `time.sleep`（行 88、129）→ 改为 `wait_scene_stable` 等价的像素差判据
- 验收：`recognize.py` 缩减至 ~500 行、仅保留 `template_matching`；`find()` 的 180 处调用结果与旧版一致
- ⚠️ AGENTS.md 核心原则 3：`utils/` 核心逻辑**不改**，仅允许识别层收敛与分辨率适配（REFACTORING_PLAN.md §1.7）

**S15 官方步骤 5：Full Chain**

- `MainLoop` + planners + executors + database 端到端；`hooks` 接线收口
- 验收：空队列不空转、任务按时间出队、异常不上抛、快照可存可恢复

**S16 官方步骤 6：Copilot**（`scheduler/copilot/**`，`deployer.py` 有 3 处 `NotImplementedError`）

- 验收：JSON 战斗计划重放，指令序列与旧版一致

**S17 官方步骤 7：Android**

- MediaProjection + AccessibilityService 真机跑通基建
- 验收：Android 真机跑通基建

---

### 10.4 待决事项裁定（原 §八 收口）

主控已裁定下列事项。**Session 不得再自行讨论或改变方向**；若发现裁定前提不成立，报主控复议。

| # | 原事项 | **裁定** | 理由 / 回退触发条件 |
|---|---|---|---|
| 2 | BUG-2/BUG-3 翻页到底：抛异常还是返回失败信号 | **抛异常**（`StepRetry` 或专用异常） | `return []` 会被 `Dispatch` 判为成功、`state.error=False`，错误被吞。回退条件：若异常导致主循环崩溃（异常隔离失效） |
| 3 | BUG-1 修复后是否重跑 A2 | **是**，由 **S13 第 3 项**执行 | 真机补位路径从未跑通，必须覆盖 |
| 4 | A3 删假任务后启动行为改变 | **接受**删除 | 启动自动换 `room_1_1` 是调试残留，非产品行为 |
| 5 | 阶段 B 多任务接口方案 | **`plan_all` 纯函数式扩展**（默认等价于 `plan`） | 方案 1 破坏既有契约；方案 3 藏状态难测。风险最低 |
| 6 | `agent_selection.py`（752 行）去留 | **删除** | `run()` 零调用；已被实机验证的 `agent_swap_service.py` 取代；一次消掉超长 + 16 处 `time.sleep` |
| 7 | C5 现在修 `state.py` 回归 | **现在修**（S5） | `backup_plans=[]` 时报"宿舍优先级和当前宿舍不匹配"是**误导性**错误，掩盖真正原因 |
| 8 | 是否构建 `ui/dist` | **不构建** | 图形界面与重构主线正交；`dist/` 已 gitignore。需要时另开 Session |
| 9 | `_find_in_cache`/`_find_free_in_cache` 死方法 | **删除**（S3） | 各自零引用；补位已由 `_selected` + Free 位替换实现 |
| 10 | `MAX_PAGE=50` 是否偏小（实测约 52 页） | **保持 50**，改为"翻到末尾就停"判据 | 调大只是推迟问题；BUG-1 修复后第 0 页即补位，翻页非关键路径 |
| 11 | 验证脚本是否加"连读 2~3 次取一致值" | **加**，作为 `tests/live/` 强制规范 | 曾因单次读数误判"宿舍被改乱"，见 §四教训 |
| 14 | 是否把 `SchedulerState` 传入 `AgentSwapService` | **传入**（S3） | 无 `current_room == ""` 空闲判定与 `free_blacklist` 则语义不完全对齐旧实现；此处"从简"= 语义错误 |
| — | `executors/base.py:safe_execute` | **删除**（S2） | 零引用；与 `dispatch.execute()` 的异常隔离重复 |
| — | `LegacyPlannerAdapter` | **删除**（S2） | 零引用 |
| — | `InfraRegistry` / `hooks.py` | **删除**（S1） | 零引用 |
| — | `scheduler/REWRITE_PLAN.md` | 移到 `.opencode/`（S1） | 孤儿文档，不属产品 |
| — | `arknights_mower/tests/cuoyu.py` | 先证明零引用后**删除**（S0） | 非测试：导入即执行、标识符乱码、不被 `*_tests.py` 匹配、不入门禁 |
| — | `scripts/fix_runtime_dlls.py` | **留在 `scripts/`** | 构建工具，非测试 |

#### S0 复核后新增裁定（主控已复验，2026-09-16）

| 事项 | **裁定** | 复核结论 |
|---|---|---|
| S0 把 `agent_swap_free_fill_tests.py` 的内部局部 mock 换成 `tests/harness/MockDevicePort` | **接受** | §10.2 明文要求单测一律用 harness 夹具；4 条断言逐条保留且全绿（主控已复跑：`Ran 4 tests OK`）。该文件未跟踪，不涉及他人已提交内容 |
| S0 新增 `tests/harness/selftest.py`（13 个元测试）且**刻意不进 G1** | **接受，并提升为 G5** | 理由正当：夹具变动不应污染 35 这个**业务**基线。主控已复跑 `Ran 13 tests OK`，并要求其中"逐目录查 `__init__.py`"与"查重复加载"两条守卫各自可证伪（S0 已反证） |
| S0 指出 `-t .` 的**真实危害**与主控原稿不同（原稿称"否则 import 失败"，实为模块双载） | **接受更正** | **主控已独立复现**：缺 `-t` 时同一文件以 `unit.xxx` + `tests.xxx` 两个模块对象进入 `sys.modules`，`is` 为 `False`（文件相同）。原稿措辞错误，采信 S0 实测。缺 `-t` 时 G1 仍 `Ran 35 OK`，故**不会**报错，危害是静默的 |
| S0 未把 `agent_swap_free_fill_tests.py` 放进 `tests/unit/scheduler/` | **维持平铺 `tests/unit/`** | 任务书表格如此要求；目录镜像 `scheduler/` 是"期望"而非硬约束，且该文件已在基线 35 中。**不为它单独改动**（避免动已通过的基线） |
| 文档中 15 处历史路径引用是否批量订正 | **不批量改**，加换算提示 | 纪律是"只增不改他人条目"；历史进度记录应保持当时原貌。已在 **§三 开头**加统一换算提示，并保留 §七/§10.1 为唯一现行权威 |
| S0 报告称"全仓 ruff 49 → 51，+1 为 cuoyu.py 的 E902" | **更正 S0 的账** | **主控实测：删除 `cuoyu.py` 前后三个 scope 均不变**（`arknights_mower` 49 / `scheduler` 38 / `.` 51）。原因：ruff 目录遍历**不读**非 UTF-8 文件（故 `cuoyu.py` 从未被计入 `arknights_mower` scope）；只有显式指定该文件路径时才报 E902。`. ` 与 `arknights_mower` 的差 2 来自仓库根 `server.py` + `server_scheduler.py` 的 I001，与 `cuoyu.py` 无关 |

#### S1 复核后新增裁定（主控已逐项复验，2026-09-16）

**S1 交付判定：通过。** 主控独立复跑门禁，5 项全部与 S1 报告**逐字一致**：
G1 `Ran 47 OK` exit 0 ｜ G2 exit 0 ｜ G3 **33** ｜ G4 exit 0 ｜ G5 `Ran 13 OK` exit 0。
三个删除目标确认不存在；`time.sleep(` 与 `^import time` 全库均 **0** 命中；
`REWRITE_PLAN.md` 已移至 `.opencode/`；四个独占文件 ruff 均为 **0**。

| 事项 | **裁定** | 复核结论 |
|---|---|---|
| S1 的 G3 = **33**，与主控预算完全吻合 | **接受，预算表获验证** | 主控预算 S1 占 5 个错误（`bootstrap.py` 3 + `agent_selection.py` 2），38−5=33。S1 额外自清了 `bootstrap.py` 的 F401，故其 4 个文件全部归零，**未替他人背账** |
| `__main__.py` 32 → **69 行**（+37） | **接受** | 增长来自补 docstring（说明 `test-arrange` 新语义 + 等待策略）。文件远低于 300 行上限，且消除了 `time.sleep(5)` 违规。**净收益为正** |
| S1 请求裁定：`__main__.py` 的 `time.sleep(5)` 替代方案是否需要新常量 `SCENE_POLL_INTERVAL` | **不需要，维持 S1 现有实现** | S1 已改用 `navigator.wait_scene_stable()` —— **正确选择**：① 零新常量（避开 §10.5 的 `constants.py` 单点写入约束）；② 语义更准（由截图像素差判定界面就绪，而非与画面无关的固定 5 秒）；③ 符合 AGENTS.md 规则 6。**不追加 `SCENE_POLL_INTERVAL`** —— "从简"原则：现在没有第二个使用方，加它就是为未来预留 |
| `.opencode/` 等文档仍描述已删除模块 | **不处理** | 非代码、非门禁范围，且不在任何 Session 独占集。若日后需要，另开文档整理任务 |

**⚠️ 主控发现并修复的一处跨 Session 冲突（重要）**

S1 的测试 `bootstrap_cleanup_tests.py` 原含一条断言：

```python
self.assertNotIn("InfraScanPlanner", source)   # 已删除
```

**该断言会在 S6 合法接线时变红。** 主控已实测复现：把 `InfraScanPlanner` 真正注册进
`_build_planners` 后（这正是 S6 的任务 —— "注册全部已迁移 planner"），该用例报
`FAILED (failures=1)`。而 `planners/infra_scan.py` 是**合规** planner
（`condition` + `make_task`，39 行），S6 注册它是**正确的**。

**冲突根因**：S1 的意图是"不得留注释死代码"，但断言写成了"该符号不得出现"，
把"注释掉的接线"与"有效接线"混为一谈。

**主控已修正**（改 `tests/unit/scheduler/bootstrap_cleanup_tests.py`，不改 `bootstrap.py`）：
拆成两条**意图精确**的用例，替代原来那条过宽断言：

| 新用例 | 断言 | 可证伪性 |
|---|---|---|
| `test_no_commented_out_planner_in_bootstrap` | 源码中不得有**以 `#` 开头且含 `Planner`** 的行 | ✅ 已反证：注入 `# planners.append(InfraScanPlanner())` → 变红 |
| `test_build_planners_has_no_commented_wiring_ast` | AST 级：`_build_planners` 内 `append` 调用 **≥1** | ✅ 防"整段注释掉" |

**主控已验证修正效果**：注入 S6 式**合法**接线（真注册、无注释）后，
`bootstrap_cleanup_tests` 保持 **`Ran 14 OK`**（修正前为 FAILED）。
即 S6 今后可自由接线而不被误报。

> **给 S6 的提示**：你注册 `InfraScanPlanner` 是**允许且被期望**的。
> 若你在本 Session 期间看到该类断言失败，请先确认它是否已被主控修正；
> **不要**为了迁就一条过宽的断言而放弃接线。

#### S2 复核后新增裁定（主控已逐项复验，2026-09-17）

**S2 交付判定：通过。** 主控独立复跑门禁，5 项全部与 S2 报告**逐字一致**：
G1 `Ran 74 OK` exit 0 ｜ G2 exit 0 ｜ G3 **32** ｜ G4 exit 0 ｜ G5 `Ran 13 OK` exit 0。
另经**逐字节验证**：`Step` 的四个字段（`name`/`enter`/`act`/`start`）与搬迁前
**AST 完全一致**（`FIELDS IDENTICAL: True`），非重写；
三个再导出均为**同一对象**（`B.Step is S.Step` → `True`），未退化为子类副本。
`agent_swap_service.py` 等三个引用方在 S2 的两个提交内**零改动**（实测 `git diff --name-only` 为空）。

| 事项 | **裁定** | 复核结论 |
|---|---|---|
| ★ **裁定请求**：§10.1 硬指标 5（触碰文件 ≤300 行）与"标准 10（新增测试覆盖 4 项）"冲突，S2 把测试拆成两个文件 | **S2 处置正确，规格无需修改** | 已核对 §10.1 第 888 行原文**明文写着**"拆出的新文件同样受 300 行约束"，故无矛盾 —— 300 行约束**本就含测试文件**。S2 按"静态/AST vs 行为"性质拆分（230 + 243），是**按内聚性拆**而非凑数，**接受**。主控已在硬指标 5 补注"含新建文件、含新建的测试文件"以消除歧义。**不豁免测试文件** |
| S2 主动发现的测试弱点（证伪 №2） | **表扬并要求全员效仿** | 第一版"逐字 grep 等价"断言被 `670 / 1920`（**空格包围**）绕过，用例**假绿**。S2 自己证伪出来并补了 AST 级守卫（按 `ast.Constant` 判断除数，`SCREEN_W` 属 `ast.Name` 放行）。**主控已复现**：注入空格写法 → 该守卫 `FAIL`（指向 `test_executors_base_has_no_division_by_raw_resolution_ast`）。这是"**证伪验证**"环节的真正价值 |
| `constants.py` 157 行，Wave 1–3 单点写入归 S2 | **确认，规则不变** | S2 已按 §10.5 规则 3 提出"后续 Session 需追加常量时应在报告中提交，不要自行改"。主控**确认此约定继续有效**，并**接收 S2 的移交**：`constants.py` 的单点写入权自 S2 收工起**回到主控**，后续 Session 需要常量一律先问主控 |
| S2 报告的行数起点写 `4928` | **更正为 `4935`** | 账算不平：`4928 + 19 = 4947 ≠ 4954`；而 `4935 + 19 = 4954` ✅。真实起点是主控 S1 后实测的 **4935**（S0/S1 报告中的 4928 是旧值，此差异**主控已在 S1 复核时记录**）。**终点 4954 正确**，仅起点数字继承了一个过期值。非 S2 过失 |
| `services/ → executors/` 倒挂未彻底消除 | **接受，维持原计划** | S2 按规约只做"移动 + 再导出"，`agent_swap_service.py:34` 仍从 `executors.base` import —— 那是 **S3 的独占文件**。S2 **正确地没有越界**。倒挂在 S3 改该行后自然消除 |
| `TRADE_ORDER_AGENTS` 5 处散落定义未动 | **接受** | 属 S5/S6 的活（`planners/exhaust.py`、`planners/order.py` 属 S6；`state.py` 属 S5）。S2 只建权威定义，**正确地没有替他人改文件** |
| `MAX_PAGE`、`AbstractPlanner` 多任务接口未动 | **接受** | 两者分别属 S3 与 S6 的范围。S2 **只删死代码**，未提前动接口 —— 符合"从简"与独占边界 |

**S2 交付的关键依赖已就位（S5/S6 依赖项）**：

```python
from arknights_mower.scheduler.constants import TRADE_ORDER_AGENTS
# ["但书", "龙舌兰", "佩佩", "可露希尔"]
```

主控已实测该 import 可用。**S5/S6 现在可以放心引用**，替换各自文件内的散落字面量。

#### S3 分派前发现的三处规格缺陷（主控实测更正，2026-09-17）

主控在写 S3 提示词前**通读 `agent_swap_service.py` 全文**，发现原规约有**三处会误导执行者**，
已全部更正。这三处若不修，S3 会**照着一份错的规格施工**。

**缺陷 1：BUG-2 与 BUG-3 是同一处代码，原规约当成两件事**

用 `git show 1d1463e8` 比对重构前的实现，`_do_select` 里原本是**一条语句**同时承担两件事：

```python
cur_names = [c[0] for c in self._cache]
if cur_names == self._last_names:          # ← BUG-3：这个比较被删了
    logger.error(f"AgentSwap: reached end of list, target={target} not found")
    return []                              # ← BUG-2：静默返回
self._last_names = cur_names
```

**「翻到底检测」（BUG-3 丢失的比较）与「静默返回」（BUG-2）本就是同一条 `if`。**
原规约把它们拆成两条独立任务，会导致 S3 只修 BUG-2 的返回值、
却**没有"翻到底"的判据**可用，最后只剩 `MAX_PAGE` 兜底。
**裁定：必须一并修**，并已在 S3 验收标准里增设 **2b**。

**缺陷 2：`StepRetry` 会导致无限循环（原裁定给的两个选项之一是错的）**

原裁定写"抛 `StepRetry`/专用异常"。主控实测：**`StepRetry` 在此处会死循环**。

`AgentSwapService._run_steps` 与 `AbstractExecutor.run_steps` **不是同一套实现** ——
它**没有 `guard()`、没有超时兜底**：

| 抛出 | 实际结果 |
|---|---|
| `StepRetry` | `:146` `continue` → **队列不推进** → `queue[0]` 仍是同一个 `select` step → 再抛 → **无限循环，无超时能救** ❌ |
| `StepRestart` | `:148` 重置队列 → 重跑 → 同样条件 → **同样无限循环** ❌ |
| **普通异常** | `:150 except Exception` → `return False` → `shift.py:130 raise StepRestart` → **外层** `AbstractExecutor.run_steps` 有 `guard()`（`_timeout=10min`）兜底 → `dispatch` 捕获 → `loop.py:91 state.error = True` ✅ |

**裁定：新增 `AgentSwapError(Exception)` 并抛它。禁止抛 `StepRetry`/`StepRestart`。**
（这同时满足"错误必须可上报"与"不得死循环"两个约束。）

**缺陷 3：空闲判定的方向写反了**

原验收标准 5 写"`current_room == ""` 的干员**被排除**出候选"。主控核对 legacy
`base_schedule.py:2681-2709` 的 `get_free_list` 实现：

```python
free_list = [v.name for k, v in self.op_data.operators.items()
             if v.name not in agents
             and v.operator_type != "high"
             and v.current_room == ""]        # ← 这是**入选**条件
```

**`current_room == ""`（空闲、不在任何房间）恰恰是自由人的定义 —— 是"要选它"而非"排除它"。**
真正要排除的是 `current_room != ""`（已在别处工作）的干员，以及 `free_blacklist` 内的。
**已更正验收标准 5 的方向。**

> **顺带确认当前实现的真实缺陷**：`_do_select:225` 用
> `if name not in self._agent_list or name in self._selected: continue` 做筛选，
> 而 `_agent_list` 是**全体干员名集合**（`__init__:61` 由 `data.agent_list` 而来）。
> 该条件几乎恒为 `False`，**等于没筛** —— 这正是裁定 14（注入 `SchedulerState`）
> 要修的语义缺失。**注入 `state` 的目的就是拿到 `operators[...].current_room` 与
> `config.free_blacklist`，把这条恒假判断换成真正的空闲判定。**

**另：`BUG-3 已修` 的说法是错的**

原 S3 初步引导写"BUG-1/BUG-3 已修，勿回退"。主控实测：`_last_names` 当前在
`:114 / :216 / :257` **3 处全是赋值、0 处比较** —— **BUG-3 根本没修**。
**已更正为"BUG-3 尚未修复，它是你的活"。**

**`_find` 不得删除（原规约未提及，但有误删风险）**

原规约只要求删 `_find_in_cache` / `_find_free_in_cache`（各 1 处定义、零调用 ✅）。
但同一区域还有个 `_find`（`:484`），它**有 8 处调用**（`:396,397,416,417,430,431,566,570`）。
S3 若"顺手清理相邻的死方法"就会误删它。**已在验收标准 6 显式写"`_find` 必须保留"。**

---

#### S3 复核裁定（主控独立复验，2026-09-17）

**结论：S3 通过。** 5 条门禁**逐项复现一致**，行为等价性经主控**独立验证**（未采信 S3 自述）。

**门禁复现（主控实跑）**

| 门禁 | S3 自述 | 主控实测 | 判定 |
|---|---|---|---|
| G1 | `Ran 97 OK` | `Ran 97 tests OK`, exit 0 | ✅ |
| G2 | exit 0 | exit 0 | ✅ |
| G3 | 24 errors | **24 errors** | ✅ |
| G4 | exit 0 | exit 0 | ✅ |
| G5 | `Ran 13 OK` | `Ran 13 tests OK`, exit 0 | ✅ |

**行数逐项吻合**：S3 声称的 10 个文件行数（113/184/155/41/99/108/116/135/156/195）
**全部逐项等于实测值**。`scheduler/` = **69 文件 / 5,315 行 / 超长文件 2 个**
（`state.py` 572 → S5、`navigator.py` 450 → S4）。与自述完全一致。

**行为等价性 —— 主控独立验证（本 Session 的验收核心）**

S3 自述用临时 worktree 做了场景级 tap/swipe diff。主控**另建一套不依赖任何 mock 场景
构造**的验证：把拆分前 `agent_swap_service.py` 的**每个方法体**与拆分后对应方法
按**方法名**（拆分把方法移入不同 Mixin，类名合法变更）做 **AST 逐字节比对**（剥离 docstring）：

| 结果 | 数量 | 说明 |
|---|---|---|
| **方法体逐字节相同** | **24 / 30** | `_do_prepare`、`_do_scan`、`_do_sort`、`_find`、`_run_steps`、`_run_train`、`_scene_check`、`_tap_*`、`_read_name` 等**纯搬迁** |
| 方法体不同 | 6 | 全部**预期内**，逐项复核见下 |
| 精确删除 | 2 | `_find_in_cache` / `_find_free_in_cache`，**正是裁定要删的两个** |
| 新增 | 5 | `_check_end_of_list`、`_advance_page`、`_is_free_candidate`、`_find_free_candidate`、`dedup_agents` |

6 处差异**逐项核对结论**：

| 方法 | 差异性质 | 裁定 |
|---|---|---|
| `__init__` | 注入 `state` + 会话状态字段预置初值 | ✅ 裁定 14 |
| `_do_select` | BUG-2/BUG-3 修复（抽成 `_check_end_of_list` + `_advance_page`） | ✅ 预期 |
| `run` | 抽 `dedup_agents`；`f""` → 纯字符串；`self._found_target = False` 补初始化 | ✅ 等价（`dedup_agents` **保留了 `deepcopy`**，调用方列表保护未丢） |
| `_switch_filter_other` | `l` → `label` | ✅ **纯改名**，AST 语义等价（即 ruff E741） |
| `_read_operators_on_screen` | 三元 → `if/else` | ✅ **语义等价**（主控提取 test/then/else 三分支**全部 equal**，即 ruff E501） |
| 同上，切片空格 | `[y[0]:y[1]]` → `[y[0] : y[1]]` | ✅ 纯 PEP8 |

> **即：6 处差异全部是"计划内修复"或"ruff 格式化"，无一处行为漂移。**

**主控对自身裁定的反向证伪（`StepRetry` 是否真会挂死）**

主控在裁定时给的是**推理**而非实测，S3 交付后做了**反向证伪**：

- 构造一个 step 抛 `StepRetry`，用**合法场景** `Scene.RIIC_OPERATOR_SELECT` 跑 `_run_steps`
  → **子进程 15 秒超时，确认挂死** ✅ **裁定成立**
- 同一场景抛 `AgentSwapError` → **立即返回 `False`**（无挂死）✅
- 类层级：`AgentSwapError.__mro__[1]` 是 `Exception`，**非** `StepRetry`/`StepRestart` 子类 ✅

> ⚠️ **主控自己的第一次证伪是无效的**，记录以免后人重犯：最初用 `get_scene` 返回 `0`
> 探测，但 `_run_steps:104-111` 对**非白名单场景**直接 `return False`
> （"unexpected scene, abort"）——**step 根本没被执行**，于是得出
> "抛 `StepRetry` 也返回 False"的**假阳性**。必须用合法场景才能测到真实循环行为。

**S3 自曝问题的复核**

| S3 自述 | 主控复核 | 判定 |
|---|---|---|
| 夹具陷阱：`!= ""` 与 `== ""` **同字节数**，同秒还原会让 CPython 复用按 `(mtime,size)` 校验的陈旧 `.pyc`，造成"还原后仍红"的**假红** | **真实且重要**。这是 Python 字节码缓存的标准行为（`(mtime, size)` 双键），S3 自建证伪脚本时踩到并修正 | ✅ 归档 |
| G1 有 2 次 `Logging error / TypeError: not all arguments converted`，属**既有**噪声 | 主控在 `1688bbd5`（S3 前）worktree 实跑：**同样 2 次**，`Ran 74 OK` | ✅ **证实** |
| `agent_swap_base.py` 的 `_profession_filter_names` 只赋值、零读取 | 属拆分前既有状态，S3 未擅自扩大改动面 | ✅ 得当 |

> ⚠️ **主控复核实跑中的一次自身失误**（归档）：复核"既有噪声"时首次传了**剥离的 `env`**
> （仅 `PYTHONIOENCODING`，丢了 `PATH`/`SYSTEMROOT`），导致 worktree 内跑出
> `Ran 50 tests / FAILED (errors=4)` —— **假基线**。改用 `dict(os.environ)` 继承后
> 得 `Ran 74 OK`，与真实基线一致。**教训：跨 worktree 起子进程必须继承完整环境。**

**遗留项裁定**

1. `constants.py` 未被改动 ✅ 正确（S3 无新增常量需求，写入权在主控）
2. `_profession_filter_names` 零读取 —— **留给 S6**（Wave 2 拥有该文件清理权）
3. G3 剩余 24 个错误全属他人文件 —— **符合预算**（S4 ≤17 / S5 ≤13）
4. 2 次 `Logging error` 噪声 —— **已确认既有**，不计入 S3 责任

**S3 交付的关键依赖已就位**

```python
from arknights_mower.scheduler.services.agent_swap_errors import AgentSwapError
from arknights_mower.scheduler.services.agent_swap_service import AgentSwapService
```

`AgentSwapService` 现为 Mixin 组合，构造签名新增 `state: Optional["SchedulerState"] = None`
（**向后兼容**：`state=None` 时退化旧行为，故 BUG-1 回归网不受影响）。

#### S4 分派前发现的三处规格缺陷（主控实测更正，2026-09-17）

主控在写 S4 提示词前通读 `navigator.py`（450 行）全文，发现原规约**三处会让执行者踩坑**。

**缺陷 1：`_action_*` 是 `getattr` 字符串分派 —— 用组合拆分会让 55 个动作全部失效**

`navigator.py:62`：

```python
handler = getattr(self, f"_action_{transition.action}", None)
if handler is None:
    logger.error("no handler for ...")
    return False
```

实测 `_action_*` 共 **55 个**（合计 126 行，占 L271-450 **连续尾部块**）。
若按"组合"把它们移到另一个对象上，`getattr(self, ...)` 会**全部返回 `None`**
→ 55 个动作同时报 `no handler` → **导航全面失效**。
**裁定：必须用 Mixin/继承，`_action_*` 仍绑定在 `Navigator` 实例上**（S3 已用此法，可参考）。

> ⚠️ 这是一个**"拆分方式选错就静默坏掉"**的陷阱：组合与 Mixin 在静态检查、
> 导入、甚至"方法存在性"上看起来都对，**只有运行 `getattr` 分派时才会暴露**。
> 故 S4 验收标准新增 **3b**：断言 55 个 action 名**全部可取到**处理器。

**缺陷 2：验收标准 6 的 grep 判据不可能通过**

原标准 6 要求 `grep "time\.sleep\|import time" navigator*.py` **无输出**。
但 `navigator.py:178` **确实有** `import time as _time`，用作 `wait_scene_stable`
的**超时预算**（`t0 = _time.time()` / `while _time.time() - t0 < max_duration`）。
`time.sleep(` **零命中**（正确），但 `import time` **必然命中** → 判据**永远无法通过**。
AGENTS.md 规则 6 禁止的是 `import time` **+ `time.sleep()`** 的组合，计时用途不在禁止之列。
**已把标准 6 改为"`time.sleep(` 零命中"，并显式许可 `_time.time()` 用于超时预算。**

**缺陷 3：建议的 `navigator_graph.py` 没有代码可放**

原建议写 `navigator_graph.py`（路径搜索），但 `navigator.py` **不含路径搜索实现** ——
它只在 `navigate:57` 调用 `self._graph.find_path(...)`，而 `find_path` 定义在
**`graph.py:194`**（非 S4 独占文件）。**该文件是空目标，已移除。**

**更正后的拆分面（实测行数）**

| 目标文件 | 内容 | 实测 |
|---|---|---|
| `navigator.py` | L1-270：`__init__`(13) `navigate`(46) `enter_room`(38) `_detect_room`(30) `_detect_digit`(11) `wait_scene`(3) `wait_scene_stable`(34) `_wait_room_detail`(22) 及点击/返回原语 | **~270** ⚠️ 贴近上限 |
| `navigator_actions.py` | `_action_*` **Mixin**，55 个方法 | **~67** |

> 核心 ~270 行贴近 300 上限；若加 import/注释后超限，**再抽**
> `navigator_wait.py`（`wait_scene` + `wait_scene_stable`，共 37 行）。

#### S4 复核裁定（主控独立复验，2026-09-18）

**结论：结构改动全部通过，但 G1 不稳定 —— 有条件通过，须先修 flaky 用例。**

**✅ 已独立复现并确认无误的部分**

| 项 | S4 自述 | 主控实测 |
|---|---|---|
| 行数（5 个文件） | 288 / 202 / 214 / 207 / 116 | **逐项吻合** |
| `scheduler/` | 70 文件 / 5,355 行 / 超长 1 | **完全一致**（仅剩 `state.py` 572） |
| 裸 `1920`/`1080` | 0 | AST 常量节点扫描：两文件均 `[]` ✅ |
| 55 动作可分派 | 55/55 | `getattr` 实测 **55/55 可取到**，MRO = `Navigator → NavigatorActionsMixin → object` ✅ |
| 构造签名 | 未变 | `inspect.signature` 逐参数一致 ✅ |
| 未碰禁改文件 | 是 | `git diff --name-only eb4a80c1..HEAD` **无** `bootstrap.py`/`__main__.py`/`graph.py`/`constants.py` ✅ |
| `time.sleep(` | 0 | 0 ✅（`import time as _time` 按裁定保留） |
| ruff | 17 | **17** ✅ |

**行为等价性 —— 主控独立 AST 逐方法体比对**（与 S3 同法）：
拆分前后**各 70 个方法**，**65 个方法体逐字节相同**、**0 新增**、**0 删除**，
仅 **5 个**不同 —— 与 S4 自述的 5 个**完全一致**，且逐项核对为：

| 方法 | 差异 | 判定 |
|---|---|---|
| `enter_room` | 9 处 `1920`/`1080` → `SCREEN_W`/`SCREEN_H` | ✅ 等价 |
| `_tap` | 1 处 | ✅ 等价 |
| `_action_announcement` | 1 处 | ✅ 等价 |
| `_detect_room` | 删掉未使用的函数内 `import numpy as np`（F401） | ✅ 等价 |
| `wait_scene_stable` | 函数内 import 重排（I001） | ✅ 等价 |

> **即：拆分是"纯搬迁 + 常量替换 + ruff 清理"，无行为漂移。**

**❌ 未通过项：G1 存在 flaky 用例（S4 自述 `Ran 121 OK` 不可稳定复现）**

主控首次复跑即得 **`Ran 121 tests ... FAILED (failures=1)`**，而 S4 报告写 `OK`。
定性实验（**8 次全量** + **10 次单模块** + **12 次直接调用**）：

| 运行方式 | 结果 |
|---|---|
| 全量 G1 连跑 8 次 | **4 次 FAILED** |
| 单跑 `navigator_contract_tests` 10 次 | **3 次 FAILED** |
| 单跑该用例 10 次 | **9 次 FAILED** |
| 直接调用 + 计轮次 12 次 | 9 次 `True` / 3 次 `False` |

失败用例恒为新增的
`tests/unit/scheduler/navigator_contract_tests.py:53`
`WaitSceneStableTests.test_crop_limits_compared_region`，
断言 `assertFalse(uncropped)` 报 `AssertionError: True is not false`。

**根因（主控用真实 `wait_scene_stable` 计数证实，非推测）**

该用例的**否定对照** `uncropped` 用 **10 帧** + `max_duration=0.2, min_stable=2`。
而 `MockRecognizer.advance_frame()` **钳制在最后一帧**，因此循环走到第 9 帧后
**相邻帧变相同** → 稳定计数开始增长 → **第 11 轮**达成 `min_stable=2`。

实测：0.2 秒内循环恰好跑 **10~11 轮**（min=10 / max=11 / 均值 10.8），
且 **`returned=True` 当且仅当轮次 ≥ 11**。

> **即：断言结果取决于"0.2 秒内 CPU 能跑 11 轮还是 10 轮" —— 这是时序竞态，不是实现缺陷。**
> 机器空载/单跑时更容易凑够 11 轮 → 断言失败；全量跑时有争用 → 轮次不足 → 反而通过。
> 这解释了"单跑 9/10 失败、全量 4/8 失败"的反直觉分布。

**已按此新增 §10.2 硬要求 5「禁止墙钟依赖」**，并规定 **G1 必须连跑 ≥3 次**。

**次要发现（不阻塞，记录备查）**

同文件 `test_changing_frames_return_false_on_budget` 用 **400 帧**规避钳制 ——
方向正确（结构性不成立），但 400 × 5.93 MiB ≈ **2.32 GiB** 峰值内存。
可用但不经济；修正 flaky 用例时应优先"循环帧"方案，**不要**复制"海量帧"写法。

**处置**：**已发回 S4 修复**（主控另行下发提示词）。修好之前 S4 计入"有条件通过"，
不计入完成；基线数字暂按 S4 实测值登记，但 **G1 以"连跑 ≥3 次全绿"为生效条件**。

#### S4 补交复核裁定 —— 有条件通过 → **正式通过**（主控独立复验，2026-09-18）

**结论：flaky 已消除，且修法是"结构性"的而非"运气好"。S4 正式通过。**

**① 范围正确**

补交 commit `b52dda0a` 只含 3 个文件：2 个测试 + `重构流程.md`。
- **生产代码零改动** ✅（`arknights_mower/` 无 diff —— 与 S4 声明一致）
- **共享夹具零改动** ✅（`tests/harness/**` 无 diff；`CyclingRecognizer` 放在
  S4 自己的 `navigator_scenarios.py`，未污染 S0 的跨 Session 文件）

**② 高频复跑（主控独立执行，比 S4 更狠）**

| 验证 | S4 自述 | 主控实测 |
|---|---|---|
| 单用例连跑 | 30/30 | **60/60 全绿** |
| 全量 G1 连跑 | 11 次 | **10/10 全绿** |
| 模块内另 3 个用例（各 25 次） | 未跑 | **0 失败** ✅ |

> ️ 复跑**本身不足以证明**修复 —— 原 bug 单跑失败率仅约 1/20，
> "连跑 60 次全绿"也可能只是负载恰好合适。故主控继续做了下面的**结构性验证**。

**③ 结构性验证（决定性，主控自建）**

复跑只能证明"这次没红"，不能证明"不可能红"。主控用 **非 `time.sleep` 的忙等**
人为把循环拖慢（这正是原来的竞态变量），观察结论是否随轮次变化：

| 人为节流 | 预算 | 轮次 | `uncropped` |
|---|---|---|---|
| 无 | 0.2s | 11 | `False` |
| 无 | 2.0s | 100 | `False` |
| 0.01s/轮 | 0.2s | 7 | `False` |
| 0.05s/轮 | 0.2s | **3** | `False` |
| **0.2s/轮** | 0.2s | **1** | **`False`** |
| 0.2s/轮 | 2.0s | 9 | `False` |

**把循环从 100 轮拖到 1 轮，结论不变** —— 证明 `False` 来自**结构**
（相邻帧恒不同），与墙钟**完全无关**。这正是 §10.2 硬要求 5 要的形态。

**④ 新写法真的守得住陷阱（最关键的一条）**

主控把否定对照**还原成修复前的旧写法**（10 帧 + 钳制），在**宽裕**预算 2.0s 下实测：

| 旧写法 + 预算 | 结果 | 说明 |
|---|---|---|
| 2.0s | **6/6 `True`** | → `assertFalse` **确定性变红** ✅ |
| 0.2s | 6/6 `False` | → 旧写法在极小预算下"碰巧通过"，**守不住** |

> **即：若只把预算调小（而不换成循环帧），陷阱会重新潜伏。**
> S4 选择"宽裕预算 + 循环帧"，使旧写法**必然**变红 —— **这是最强的回归守卫**，
> 也是主控在提示词中强调"否定对照要给宽裕预算"的原因。S4 理解并执行到位。

**⑤ 残留余量（记录，非缺陷）**

正向断言（`cropped` 应为 `True`）本质需要真实采样 3 帧，**无法做成结构性的**。
实测 2.0s 内循环可跑 ~68 轮，而正向只需 3 轮 → **余量约 23×**。
即机器需比本机**慢 23 倍**才会假红。**可接受**，已如实记录。

**⑥ 附带收益**：内存降 200 倍

`test_changing_frames_return_false_on_budget` 原用 **400 帧**
（400 × 5.93 MiB = **2.32 GiB**）规避钳制，改为 `CyclingRecognizer` 后
**2 帧 = 11.87 MiB**，**降 200×**，语义不变（帧恒变 → 永不稳定）。
S4 主动做了主控标为"可选"的这项，判断正确。

**⑦ 未擅自越界**

S4 明确报告：`MockRecognizer` 若要原生支持循环帧属改 `tests/harness/**`，
**按规范先问主控，未擅自修改** —— 边界意识正确。

**综合判定：S4 正式通过。** 基线正式生效：
`scheduler/` **70 文件 / 5,355 行 / 超长 1**（仅 `state.py` 572）、
ruff **17**、G1 **121**（连跑 ≥5 次全绿）。

---

### 10.5 并行与冲突规则

1. **文件独占（仅限同一 Wave 内）**：表中"独占文件"是**该 Wave 内的排他写权限**。同 Wave 内不得有交集（本表已按此设计并核对）。**Wave 之间是顺序执行的**（下一个 Wave 开工时上一个已合并完毕），故跨 Wave 复用同一文件**不算冲突**
2. **跨 Session 配合**：需要**同 Wave 他人**文件配合时，**在该文件用一行机械 import 保持兼容**（如 `executors/base.py` 再导出 `Step`），或登记依赖由主控协调 —— **不得直接改**
3. **`constants.py` 单点写入**：**Wave 1–3 期间归主控**。S2 收工后写入权已交回主控。其他 Session 需新增常量 → 在报告"需要主控裁定"里写明**常量名、值、用途**，由主控统一追加
4. **`bootstrap.py` 两段所有权**：S1（Wave 1）拥有整体；S6（Wave 2）只拥有 `_build_planners` 函数体。S7–S12 **不得**改 `bootstrap.py`（注册已由 S6 完成）
5. **`loop.py`**：S6（Wave 2）拥有；S15（Wave 5）之后可再改（顺序执行，无冲突）
6. **合并顺序**：同一 Wave 内 Session 可任意顺序合并（文件不相交）；跨 Wave 必须按 Wave 顺序
   > ⚠️ **两条"顺序敏感"的注意点**（主控已核对，均**不**构成死锁，但报告措辞必须对）：
   > - **S2 ↔ S3**：S2 把 `Step` 下沉到 `steps.py` 并在 `executors/base.py` **再导出**，
   >   因此 `executors/shift.py`（**S3 拥有**）无需改动。若 S3 先合并，S2 的"未改他人文件"证据
   >   必须按**自己的 diff** 判定，不能按"文件在 HEAD 里没变"判定（见 S2 标准 3 的注解）。
   > - **S2 → S5 / S6**：`constants.py` 的 `TRADE_ORDER_AGENTS` 由 S2 预置，S5/S6 改为 import。
   >   若 S2 后合并，S5/S6 的 import 会短暂失败 —— 故 **S2 应在 Wave 1 内优先合并**。
7. **基线回写**：每个 Session 完成后在主控处登记新基线数字（文件数、总行数、>300 行数、ruff 数、测试数），后续 Session 以**最新基线**为准
8. **回退单位**：每个 Session 是一个独立 commit，出问题按 Session 整体回退，不做部分回退
9. **Wave 0 特例**：S0 阻塞全员 —— **不接受任何 Wave 1+ Session 在 S0 合并前开工**（否则各自造测试目录，必然冲突）

#### 串行执行（用户实际采用的节奏：S1 → S2 → S3 → S4 → S5）

**主控已确认串行是合法且更安全的执行方式**，因为 S1–S5 的文件集两两不相交，
并行与串行的**结果集完全相同**，只是串行少了合并冲突的可能。

| 次序 | 唯一硬约束 | 主控核对结论 |
|---|---|---|
| S1 → S2 | 无 | S1 只动 `bootstrap.py`/`__main__.py`/`infra/__init__.py`/`errors.py` + 删 3 个模块；S2 只动 `steps.py`/`executors/base.py`/`planners/base.py`/`constants.py`。**交集为空** |
| S2 → S3 | **必须 S2 在前** | S5/S6 要从 `constants.py` import `TRADE_ORDER_AGENTS`（S2 写入）。S3 本身不依赖 S2（`Step` 再导出使 S3 不改也能跑） |
| S3 → S4 | 无 | 交集为空 |
| S4 → S5 | **必须 S2 在前**（S5 要 import 该常量） | S5 与 S4 交集为空 |

**串行专属优势**：每个 Session 开工时基线都是**上一个 Session 的最新值**，
因此 §10.1 的"各 Session ruff 预算"表可以逐级收敛，且不会出现"谁为谁的错误背账"的争议。

**串行下需要主控逐步回写的内容**（并行时由用户自行合并，串行时由主控在每个 Session 后更新）：

| 指标 | 起点 | S1 后（**实测**） | S2 后（**实测**） | S3 后（**实测**） | S4 后（**实测**） | S5 后 |
|---|---|---|---|---|---|---|
| `scheduler/` 文件数 | 64 | **61** | **62** | **69** | **70** | ~72 |
| `scheduler/` 总行数 | 5,678 | **4,935** | **4,954** | **5,315** | **5,355** | — |
| >300 行文件数 | 4 | **3** | **3** | **2** | **1** | **0** |
| `ruff`（scheduler） | 38 | **33** | **32** | **24** | **17** | ≤13 |
| G1 测试数 | 35 | **49** | **74** | **97** | **121** | ≥121 |

> **说明**：
> - 加粗列为**主控实测值**，可直接作为下一 Session 的起始基线。
> - `ruff` 列已与 **§10.1 的权威预算表对齐**（≤24 / ≤17 / ≤13）。
> - G1 测试数**只增不减**，故后续列为下限；不为后续 Session 编造用例数。
> - 主控在收到每个报告后**立即回写**，而不是等全部做完 —— 否则后续 Session 会拿到过期基线。
> - S3 的 `+7` 文件与 `+361` 行、S4 的 `+1` 文件与 `+40` 行**全部来自拆分**，不是新增功能。
> - **G1 自 S4 起必须连跑 ≥3 次全绿**（§10.2 硬要求 5，因 S4 出现过 flaky 用例）。

---

### 10.6 主控（map）职责边界

**主控负责**：维护本手册与基线数字；裁定待决事项；维护 `constants.py` 的单点写入；协调跨文件依赖；每个 Session 完成后复核门禁证据并更新基线。

**主控不负责**：代替 Session 写业务代码；在 Session 独占文件内直接改动（除 `constants.py` 追加常量）。

**Session 遇到以下情况必须停下来问主控**：① 需要改非独占文件；② 发现裁定前提不成立；③ 验收标准互相矛盾；④ 发现新的基线劣化来源。
