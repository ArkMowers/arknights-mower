# 软件更新与实例恢复

入口：**Mower 设置 → 页面底部的软件更新**。与资源更新使用相同的卡片表单，直接查看版本、选择渠道、检查更新或拖入安装包；宽屏左右并排，窄屏上下排列。支持 Windows、macOS 和 Linux。

## 更新渠道

| 渠道 | 更新目标 | 适用部署 |
| --- | --- | --- |
| 正式版 | 主仓库的正式 GitHub Release | 源码、Release 独立包 |
| 公测版 | 主仓库的预发布 Release，例如 `v4.1.6-alpha.3` | 源码、Release 独立包 |
| 开发版（仅源码部署） | 所选分支的最新提交（默认 `alpha`） | 源码 |

正式版适合希望减少变动的日常使用者。公测版提前提供修复和新功能，可能不稳定。开发版比公测版更新频繁，需要自行排查源码运行问题。GitHub 的 `-alpha.N` 标签在界面中归入“公测版”；“开发版”指分支提交，并非同一个更新目标。

版本按语义版本号比较，正式版检查不会误选预发布版本。Release 更新不允许直接降级，以免旧程序读取新配置。开发版按提交判断是否需要更新。

## 源码部署

使用源码 Python 启动 `manager.py` 或 `webview_ui.py`，Windows、macOS、Linux 均可识别，不通过系统名称或 `python.exe` 后缀判断部署类型。需要 Git、Node.js/npm，并且 `origin` 指向 `ArkMowers/arknights-mower`。仅当目标提交的 `.gitattributes` 使用 LFS 时才要求 Git LFS；普通 Git 仓库不依赖它。macOS 桌面启动会补充 Homebrew/MacPorts 的常见工具目录，更新命令和恢复实例沿用检测到的 PATH。支持普通 Git 检出和 Git worktree。

正式版、公测版从对应 Release 的 tag 获取提交；开发版跟随源码版本管理中应用的分支，默认 `alpha`。更新程序会核对检查时的提交，远端变化时要求重新检查。

Python 环境不要求叫 `.venv` / `venv`，也不要求放在源码目录内。依赖安装始终指定启动 Mower 的解释器。普通索引依赖清单及相关构建配置未变化时，保留当前 Python 环境并跳过重装；包含本地路径、其他清单或 VCS 引用时重新解析依赖。需要更新依赖时：

- 已有 pip：使用 `当前 Python -m pip install -r requirements.in`。
- 没有 pip、但找到 uv：使用 `uv pip install --python 当前解释器 -r requirements.in`。uv 能给指定解释器安装依赖，不要求该环境由 uv 创建；除 PATH 外还查找用户的 `.local/bin` 和 `.cargo/bin`。
- 两者都没有：在停止实例前尝试 `当前 Python -m ensurepip` 补齐 pip，并确认可运行。无法补齐时保留当前实例运行，日志显示实际缺少的能力。

不根据“没有 pip”推断环境类型。外部虚拟环境、Conda、系统 Python 和无 pip 环境均在原环境安装，不迁移解释器、不移动共享目录。依赖变化时，项目内自有且已有 pip 的普通虚拟环境采用保留旧环境、原路径重建的失败恢复方式；支持任意名称、嵌套路径，并保留 `include-system-site-packages` 设置。运行环境中未跟踪的依赖文件不阻止更新，该目录中已跟踪的源码修改仍受保护。操作系统对系统 Python 的写入保护与实际安装工具要求仍然适用，不自动绕过。

源码更新流程：

1. 检查工作区修改，获取目标提交，按目标需要获取 LFS 内容。普通更新允许 npm 锁文件的纯元数据改写；其他本地修改需要先处理或手动选择强制更新。
2. 在安装目录外创建目标提交的临时 Git worktree，当前实例继续运行。依赖变化时，pip 将目标依赖构建 / 下载为 wheel；uv 使用隔离临时环境准备下载缓存，不修改正在运行的共享环境。
3. 在临时 worktree 中执行 `npm ci`（无锁文件则 `npm install`）和 `npm run build`。下载、依赖准备和前端构建期间均可取消；失败或取消不会停止当前实例或丢弃本地源码修改。
4. 准备成功后停止接受取消，记录所有已启动实例的参数、工作目录、名称、数据目录、端口和运行状态，请求正常退出。独立更新进程接管原网页端口，继续显示安装进度。
5. 保留原提交和前端；需要重建的项目虚拟环境保留原副本。切换源码，使用已准备的 wheel / uv 缓存离线安装依赖，并应用构建完成的页面。
6. 释放进度端口，恢复原实例和端口，等待网页服务就绪，再恢复更新前正在运行的任务。


普通更新为保留本地分支指针，目标提交使用 detached HEAD 检出，不执行 `switch -f`、`reset --hard` 或 `git clean`。这不会删除原有分支或未推送提交。后续更新仍按所选渠道进行。

工作区检查遵循 `.gitignore`，忽略的运行数据和构建目录不会阻止更新。前端生成的 `auto-imports.d.ts`、`components.d.ts` 及 `.playwright-cli/` 不纳入版本控制，生产构建不重写组件声明。`package-lock.json` 继续跟踪以固定依赖。npm 自动补充 `peer`、`license`、`funding` 等元数据或调整 JSON 排版时，只要依赖版本、地址、校验值和依赖关系均不变，普通更新就会放行，安装前恢复仓库中的锁文件，不备份这些生成数据。检查更新本身不改写文件。安装依赖使用 `npm ci`，避免再次重写锁文件。

源码部署在“下载并安装”旁提供 **强制更新**。先点击“检查更新”确定目标，再点击“强制更新”；页面弹出二次确认框，显示目标版本和重启实例数量，只有点击“确认强制更新”才提交。取消或关闭确认框不执行更新。

强制更新会获取选定的远端版本，正常停止同一安装目录下所有运行实例，再在分离 HEAD 上执行 `git reset --hard <目标提交>`。这会丢弃已跟踪文件的本地改动（包括已暂存的改动），并覆盖阻挡目标文件写入的未跟踪文件，不备份本地修改。即使安装失败后回滚程序版本，已丢弃的本地修改也不会恢复。它不会运行全目录 `git clean`，与目标文件不冲突的未跟踪文件及忽略的运行数据保持原样。

强制选项仅用于这一次手动源码更新，不保存为自动更新设置。当前已是所选版本时也可强制重新应用；不会绕过必要工具、部署方式、目标兼容性、实例退出等检查，也不允许强制降级到更旧的 Release。

### 更新进度与取消

任务启动后，软件更新卡片显示 **独立更新进度** 和 **取消更新**。独立进度页在新浏览器页面中显示阶段、Release 下载量和安装日志，关闭 Mower 原生窗口不会关闭这张浏览器页面。准备阶段继续由原网页服务提供状态；实例退出后，更新器临时沿用原端口和认证，替换期间无需另外开放端口。端口交接和新实例加载时可能短暂断连，页面自动重试并保留上次状态。

点击取消后等待更新器自己的下载 / 构建子进程退出，清理本次临时准备文件，显示“已取消更新，当前实例继续运行”。源码和 Release 下载均支持；已开始关闭实例、替换程序或恢复实例时禁用取消，以免留下半安装状态。Release 网络读操作设有超时，取消会在当前读操作返回或超时后完成。重新更新需要重新提交任务。

进度页本身只包含静态界面；状态和取消接口保留当前实例的 token 认证，取消还要求同源请求和更新专用请求头。独立进度页的 token 通过 URL fragment 传入，随后从地址栏移除，不放入进度页的请求 URL。旧启动器缺少监听地址和认证登记信息时不会尝试接管其端口；更新并重新启动到包含此功能的版本后，后续更新可使用完整进度交接。

### 源码版本管理与回滚

展开软件更新卡片内的 **源码版本管理**，可查看当前分支 / 分离 HEAD 和提交，选择官方远端分支。**目标版本** 使用一个可输入的选项框，可选择最近 20 次提交，也可手动输入完整 / 短 SHA、分支或 tag 后回车确认。浏览与检查不会切换代码或停止实例。

点击 **检查版本** 后，界面显示解析出的完整 SHA、提交说明、作者和日期。安装目标固定到该 SHA，不会因为分支或 tag 后来移动而换成其他版本。**切换并重启** 会再次确认目标提交、同一安装目录下的实例数量；可单独勾选强制覆盖本地源码修改，取消确认不执行任何切换。

可选择较旧提交或重新应用当前提交；此入口与日常正式版 / 公测版的“只更新到更高版本”检查独立。目标必须包含软件更新与实例恢复模块，早于此功能的版本会在停止实例前拒绝，仍需手动 Git 部署。

提交版本切换任务时关闭软件自动更新，并记住所选分支、切到开发版渠道，避免刚回退就被自动升回去；自动检查保留原设置，用户后续可自行开启自动更新。代码仍以 detached HEAD 检出，原分支指针和未推送提交保留。

版本切换重启所有已启动实例，保留原参数、工作目录、名称、数据目录、端口和任务运行状态。仅删除各实例数据库的 `saved_state` 运行快照，以兼容仍会读取旧缓存的早期启动器；不删除数据库或 `tmp` 目录，`mastery_plan` 专精计划及历史记录保留。**配置和数据库结构不随源码回滚**：较旧代码未必能读取较新的配置或恢复专精任务，保留计划不等于旧版本一定具备当前的自动恢复逻辑。

安装失败时恢复原提交和前端。项目虚拟环境使用原副本恢复；原地安装的外部 / 共享环境则尝试用原版本 `requirements.in` 恢复依赖，再用原解释器和参数启动。后者不保证整个共享环境与更新前完全相同；安装日志若报告依赖恢复失败，需要处理后再启动。

代理统一在 **Mower 设置 → 网络与下载代理** 中配置，详见下节；软件更新使用这套共享设置，不需要给三个实例分别填写。

源码与 Release 独立包均提供“更新后后台静默重启”选项，在线更新与上传安装共用该选项。开启后不创建启动画面和网页窗口。macOS 遵循“隐藏菜单栏图标”设置，隐藏时不创建托盘进程；后台启动也不会为不存在的启动画面或托盘创建多进程队列。其他平台在后台模式保留托盘入口。窗口子进程关闭后会被回收。关闭静默选项则正常打开窗口。

macOS 源码部署的主进程、托盘及启动画面辅助进程使用 accessory/UIElement 类型，避免出现多余的 Python Dock 图标；此行为不受静默重启开关控制。正常启动时仍创建独立的网页窗口进程，该窗口进程不调用隐藏 Dock 的逻辑。Release 独立包仅在后台模式下隐藏主进程和托盘进程的 Dock 图标。

macOS 设置页用“隐藏菜单栏图标”控制原有的 `webview.tray` 开关（反向显示），无需额外配置。重启后不创建托盘进程；关闭网页窗口后当前实例继续运行，可以用浏览器访问原来的地址。此选项按服务端系统显示，从 Windows 浏览器访问 macOS Mower 时也可设置。

## 自动检查与自动更新

软件更新卡片提供“自动检查更新”和“自动更新”，默认均关闭。打开 Mower 时检查所选渠道；开启自动更新会同时开启检查，发现新版本后按所选静默重启方式安装，并重启同一安装目录下所有运行实例、恢复原运行状态。手动在线更新、上传安装包和自动更新的重启范围相同；“仅当前实例”只适用于底部独立的进程操作按钮。关闭检查也会关闭自动更新，与资源更新的开关联动一致。

这些设置按安装目录共享。多个实例同时启动时在一分钟内只发起一次自动检查；更新或进程操作进行期间不发起自动更新。软件更新、失败恢复和网页按钮触发的重启不再次自动更新，避免失败后循环重启。自动检查结果和失败原因会显示在软件更新卡片，源码改动、依赖及目标兼容性检查与手动安装相同。

## 当前实例的重启与结束

设置页最底部提供独立的“进程操作”卡片。两个按钮只作用于当前实例，其他实例和多开管理器保持运行，也不依赖 Git、Node.js 或软件更新是否可用。

“重启 Mower 进程”先保存页面配置并请求当前任务正常停止，再使用原 Python/独立包入口、命令行参数、工作目录、实例名称、数据目录和端口恢复当前实例，并恢复原运行状态。通过 manager 启动的实例保留 manager 传入的参数，不额外启动其他实例。“结束 Mower 进程”正常退出当前实例，网页连接随后断开。

软件更新、失败恢复和此处的重启按钮，均默认以“重置缓存运行”方式重新开始原本运行中的任务，跳过旧运行快照的读取，避免跨版本缓存不兼容。原本停止的实例保持停止；实例配置、排班方案及数据库历史记录保留。普通启动入口仍使用原有的启动模式。

专精计划保存在 `@app/tmp/data.db` 的 `mastery_plan` 表中，不依赖运行快照，重置运行缓存不会删除计划：

- 训练中的计划，在重新读取训练室并确认干员、技能及倒计时后，补排收取或协助位换人任务；重复读取不会重复入队。
- 已完成训练、等待收取的计划，即使任务队列为空，也会在进入训练室核实后执行收取并更新进度。
- 尚未开始的计划保留，等待仓库扫描确认材料后重新调度，不保证重启后立即开练。
- 开启“读取心情后刷新备用排班”时，第二次启动保留本次重新读取训练室后生成的专精任务，其他排班任务按新心情重新生成。

恢复需要开启全自动专精、实例继续运行，并成功进入和识别训练室；识别失败时保留计划，等待下次读取。重置缓存会清空旧任务队列，仅存在于运行缓存中、没有持久化计划或自动生成入口的一次性任务不会原样恢复。这里的缓存重置不会删除整个 `tmp` 目录或数据库文件。

退出最多等待 90 秒，不按进程名强制关闭 Python 或其他实例。重启时页面会等待原端口重新连接。操作状态与日志保存在 `updates/<安装目录标识>/control/<操作编号>/`，与软件更新日志分开。

## 网络与下载代理

位于设置页下方、软件更新与资源更新卡片上方。

- **全局网络代理**：用于全局网络连接，例如 `http://127.0.0.1:7897`。填写后覆盖大小写的 `HTTP_PROXY`、`HTTPS_PROXY`、`ALL_PROXY`，并覆盖旧的外部域名绕过规则。留空沿用进程最初的启动环境；回环地址保持直连。此设置仅作用于 Mower 进程和它启动的子进程，不修改操作系统代理设置。
- **GitHub 下载代理站点**：例如 `https://ghfast.top/`，可自行替换为支持“代理站点/原始下载 URL”格式的站点，也可保留自定义路径前缀。用于 Mower 和 MAA 的 GitHub 安装包、资源包、热更包及原始文件（包括资源版本清单和 MAA Python 的按需下载）；清空后直接访问原始下载 URL。MAA 的官方下载 API 和 Mirror酱地址保留原始地址，通过全局网络代理连接。文件代理覆盖 `github.com`、`raw.githubusercontent.com`、`codeload.github.com` 和 `gist.githubusercontent.com`。GitHub API、源码 Git/Git LFS 不走文件加速站点，使用上面的全局网络代理。

两项可同时启用：先把 GitHub 文件 URL 拼接到下载代理站点，再通过全局网络代理连接该站点。下载站点不会代替原有的安装检查和在线 Release SHA-256 校验；手动上传 Release 仍然离线安装，无需校验文件。

全局网络代理同时支持 `socks5://` 和 `socks5h://`，SOCKS 地址需填写端口，例如 `socks5h://127.0.0.1:7897`。推荐使用 `socks5h`，让支持该协议的客户端通过代理解析目标域名；`socks5` 的解析行为遵循各网络库和外部工具的实现。GitHub 下载代理站点仍填写 HTTP(S) 地址。两种代理均沿用现有的无用户名密码地址格式。

Python 运行依赖包含 Requests 与 HTTPX 的 SOCKS 扩展，不修改 AI 客户端或传输层。Release 下载和 MAA 活动导航下载使用具备 SOCKS 支持的 Requests；源码更新会在停止实例前暂存已有的 PySocks 模块，仅向新虚拟环境的 pip 提供引导路径，避免 pip 在尚未安装 PySocks 时无法通过 SOCKS 下载自身依赖。

输入停顿或离开输入框时自动保存，两项代理用于后续连接。其他实例自动同步代理环境变量；各网络客户端沿用自身读取环境变量的机制，不修改或替换 AI 客户端及其传输层。设置保存在共享的 `@app/config/network.json`，不随实例的 `conf.yml` 自动保存而被覆盖。已启动的独立软件更新任务保留提交时的代理快照；新的更新任务使用最新设置。

“测试连接”只检查 GitHub 资源包下载接口是否可达：按保存的设置拼接下载代理 URL，经全局网络代理发送 HTTP HEAD 请求并跟随重定向，显示连接结果和耗时。不下载包正文，也不解析 JSON；测试成功仅表示该下载地址的 HTTP 请求成功，不代表文件内容、下载速度或 HTTP Range 支持已经验证。

设置页按钮会先等待 `/network/settings` 保存当前输入成功，再调用 `/network/test`；保存失败时不发起测试。`/network/test` 仅测试已保存配置，不接收或保存代理输入。其他调用方也应遵循先保存、再测试的顺序。软件更新检查同样使用这套已保存配置。

## Release 独立包

在线更新按程序正在运行的系统和架构选择完整安装包，核对 GitHub 提供的 SHA-256 后安装。Release 独立包在线更新和手动上传安装均可选择后台静默重启；关闭选项时正常打开窗口。

| 平台 | 安装包 |
| --- | --- |
| Windows x64 | `arknights-mower_<版本>_windows_x64.zip` |
| Linux x64 / arm64 | `arknights-mower_<版本>_linux_<架构>.tar.gz` |
| macOS Intel / Apple Silicon | `arknights-mower_<版本>_macos_<架构>.dmg` |

macOS 必须先把 `mower.app` 移出 DMG，在可写位置运行。安装程序只读挂载 DMG、检查包内容并复制 `.app`，不修改 Gatekeeper 设置。旧式 macOS ZIP 和早期 RAR 包请自行安装。

Windows/Linux 只替换程序拥有的可执行文件和 `_internal`，保留原地的实例配置、数据库、MAA 和其他用户目录。macOS 替换 `.app`，用户数据仍在 Application Support 或用户指定的数据目录。

共享资源位于持久的 `@app/resources`，不会覆盖 internal。程序更新后按版本与声明的兼容性整包选择内置或外部资源，详见[共享资源存储与实例加载](resource-storage.md)。

### macOS 签名与系统信任

当前发布流水线只有 PyInstaller ad-hoc 签名，没有 Developer ID 签名和 Apple notarization。DMG 的镜像校验、`.app` 签名完整性、Gatekeeper 的开发者信任是不同检查：镜像或下载摘要正确，不代表系统一定允许程序启动。

更新器只读挂载 DMG，保留符号链接、权限等复制完整 `.app`，在最终待安装副本上执行 `codesign --verify --deep --strict`。检查失败时不会停止旧实例或替换程序。资源更新写在 `.app` 外，避免修改受签名保护的内容。

即使签名完整性通过，浏览器下载文件的隔离属性、系统信任策略或缺少公证仍可能导致 Gatekeeper 拦截。程序下载与浏览器下载可能带有不同的隔离属性，不能据此保证自动更新永远不被拦截。后台静默重启选项也不能免除系统允许操作。遇到拦截，需要确认包来源后通过系统“隐私与安全性”按提示处理；更新器不删除隔离属性、不禁用 Gatekeeper，也不会自行重新签名。

正式解决首次安装和更新的信任体验，需要发布方使用 Developer ID 签名、启用 Hardened Runtime、完成公证并附加公证票据。现有本地测试验证了 ad-hoc 签名复制后仍有效、修改包内资源后检查失败；未验证 Developer ID、公证或所有 macOS 版本上的 Gatekeeper 行为。

## 手动上传安装

在软件更新卡片的“手动应用”中选择官方安装包，保留原始文件名，点击“安装并重启”即可离线安装。手动上传无需校验文件，不进行 SHA-256 校验，也不联网获取校验值。平台不匹配、版本不高于当前版本或包结构不完整时不会停止实例或替换程序。

此入口适用于 Release 独立包部署。源码部署应选择在线更新中的正式版、公测版或开发版，不允许把二进制包覆盖进 Git 工作区；GitHub 的“Source code”压缩包也不是独立程序安装包。原来的热更包/资源包上传入口继续负责相应的数据包。

也可将更新包拖入主界面的任意页面：资源包、热更包 ZIP 会打开安装确认框；软件安装包会转到软件更新卡片并自动选中文件，点击“安装并重启”后才提交安装。一次仅接受一个文件。

软件 ZIP 请保留官方 `arknights-mower_…` 文件名，只有此前缀的 ZIP 进入软件安装流程；其他 ZIP 交由热更/资源包按内容识别，旧版 `mower.zip` 不支持自动软件安装。DMG 和 tar.gz 按软件包处理。

全页拖放只处理未被现有组件接管的更新包，不拦截内部排序、图片、JSON、文字拖放，也不接管现有上传区、可编辑区域和可拖拽项目。资源更新保留原有局部拖放区；Release 部署的软件更新卡片也提供“点击或拖入 Release 安装包”，可用于不支持全页文件拖放的桌面 WebView。

## 多实例、失败恢复与首次启用

一次更新覆盖同一安装目录，多个实例同时点击也只会接受一个任务。更新只恢复更新前已启动的实例；不会额外启动 `instances.json` 中原本未开启的实例。静默重启时不重新弹出多开管理器，三个实例由更新程序直接恢复；关闭静默选项则恢复原先打开的管理器。

实例有 90 秒时间正常退出；如果任务或 MAA 更新未能及时结束，则取消安装。不会按进程名关闭其他 Python、ADB 或模拟器。安装或启动失败时，更新进程尝试恢复原代码、虚拟环境和前端，并重新启动原实例。若新进程无法退出，则保留备份并报告需要人工恢复，不在仍被使用的运行环境上强行回滚。

首次启用需要先安装包含本功能的代码，并重新启动全部实例，使其注册到同一安装目录。更新目标也须包含实例恢复功能；目前的 `v4.1.6-alpha.3` 发布早于本功能。缺少恢复模块的源码或独立包会在关闭实例之前被拒绝。需要等待此功能合入后续 Release，才能完成正式发布版本之间的自动更新。

直接运行 Flask、Docker 或由其他服务管理器托管的部署继续使用原部署工具更新，避免与外部进程管理策略冲突。

更新状态、日志和源码环境备份保存在安装目录之外：

- macOS：`~/Library/Caches/arknights-mower/updates/<安装目录标识>/`
- Windows：`%LOCALAPPDATA%/arknights-mower/updates/<安装目录标识>/`
- Linux：`${XDG_CACHE_HOME:-~/.cache}/arknights-mower/updates/<安装目录标识>/`

每次任务位于 `jobs/<任务编号>/`，含 `job.json`、`recovery.json`、`update.log` 和 `restart.log`；源码备份也在该目录。独立包备份位于安装目录旁的 `.backup-<任务编号>` 目录。更新卡片显示日志路径。确认新版本工作正常后可以自行清理旧任务、运行环境副本和备份，以释放磁盘空间。

## 本地验证

2026-09-05 在 macOS Apple Silicon、Python 3.12、Node.js 22 的独立工作区验证：

- 基于 `alpha` 的 `c4da9448` 整合改动，后端全量 1013 项通过；脚本测试 84 项通过、1 项跳过；前端 44 项通过。前端生产构建、Ruff、CI 所用最新版 Prettier、修改组件的 ESLint、Python 源码编译与补丁空白检查通过。
- 临时 Git 仓库真实执行获取提交、创建虚拟环境、pip 安装、npm 构建和三个子进程退出/恢复；故意使构建失败时，确认旧提交、旧环境和旧页面被恢复。夹具使用空依赖清单，不安装游戏相关依赖或访问模拟器。
- 本机 hdiutil 创建并只读挂载临时 DMG，使用真实 ad-hoc 签名，验证 `.app` 复制、符号链接保留、替换后签名有效及回滚；修改包内资源后签名检查失败，旧实例保持运行。
- 三个真实 Python 进程固定旧资源，在各自任务边界切换到同一共享版本；覆盖基建技能 JSON 路由和窗口标题与本实例实际资源版本保持一致。
- 网络代理使用临时配置和本机 HTTP 服务器，覆盖代理环境变量替换、清空后恢复启动环境、其他实例同步、子进程继承，以及全局代理和 GitHub 下载代理叠加。MAA 程序、资源及 HTTP Range 下载均有覆盖；AI 入口保持原始客户端，不使用专用代理封装。
- 隔离预览验证设置自动保存、慢保存响应不会覆盖新输入，以及点击连接测试先应用当前输入。本机 `127.0.0.1:7897` 不可连接时，页面正确显示连接失败；成功转发使用本机模拟代理验证；另已实连 GitHub 资源包下载地址，HEAD 请求成功且不下载正文。第三方 ghfast 线路未完成实连验证。
- 软件更新与资源更新使用相同卡片表单，验证 1600px 宽屏和 390px 窄屏布局、更新检查和安装确认、在线与手动安装均准确提交开启/关闭静默重启的选项；安装提交使用测试响应，不替换当前程序。
- 浏览器验证全页资源/软件包拖放、确认后单次提交、原资源拖放区不重复提交，以及排班拖拽处理器和图片/JSON/文字拖放不被拦截。软件包定位到设置页内的更新卡片，保留局部拖放入口；原生桌面 WebView 尚未进行实际系统文件拖放验证。
- 本机 Launch Services 确认源码后台主进程和托盘进程为 UIElement；隔离实例正常关闭后子进程被回收。独立包启动模式通过模拟部署信息验证，尚未执行真实独立包版本替换。

2026-09-05 至 09-06 基于 `alpha` 的 `e5109f1a` 补充验证：

- 后端全量 1063 项通过；脚本测试 85 项中 84 项通过、1 项跳过；前端 50 项通过，生产构建通过。Ruff、最新版 Prettier 和新增组件 ESLint 通过；`Settings.vue` 的 7 项 ESLint 问题与 `alpha` 基线相同。
- 临时 Git 仓库覆盖不依赖 LFS 的源码更新、目标需要 LFS 时在停止实例前报错、遵循 `.gitignore`、显示具体修改文件，以及 macOS 工具路径传递。
- 四个真实 Python 进程模拟三个实例及 manager，重启和结束当前实例后，其余实例和 manager 的 PID 保持不变；原实例名称、含空格的路径、额外启动参数、工作目录、端口、数据目录和运行状态保留。
- 真实执行 manager 的实例启动入口，确认管理器保持打开时也会回收退出的子进程，避免将僵尸进程误判为仍在运行。
- 本机隔离桌面实例通过网页实际重启，原端口自动重连；通过网页结束后连接断开且进程退出。macOS 隐藏菜单栏并后台启动时，重启前后均只有主进程，没有托盘、窗口或多进程资源跟踪子进程。
- 软件自动更新开关联动、刷新后保存结果、自动检查去重和失败状态均有验证。验证结束时隔离实例的两个自动更新开关保持关闭。

2026-09-06 补充 SOCKS 和缓存重置验证：

- 最终后端全量 1078 项通过，脚本测试 84 项通过、1 项跳过，前端 50 项通过，生产构建通过。
- 本机 SOCKS5 协议夹具覆盖环境变量、子进程继承、Requests/HTTPX 同步和异步连接、资源与 MAA 下载、HTTP Range、Release 下载及校验失败。夹具只返回固定测试内容，不转发外网连接。
- 真实创建空虚拟环境，通过暂存的 PySocks 模块让 pip 经 SOCKS 下载测试 wheel，验证源码更新的依赖引导。
- 隔离桌面实例通过进程操作接口实际重启并在原端口重连，验证两种 SOCKS 地址均可保存、读取，代理不可连接时显示失败；测试后恢复空代理设置。此次浏览器自动化连接超时，界面沿用前一轮前端测试与构建验证。
- 临时 SQLite 数据库与空任务队列覆盖专精计划保留、待执行计划扫描后补排、训练中收取及换人恢复、待收取状态完成、识别失败时保持计划，以及读取心情后二次启动保留新生成的专精任务。设备操作使用测试替身，未连接真实模拟器验证专精。

2026-09-06 基于 `alpha` 的 `a5c406b2` 补充强制更新验证：

- 后端全量 1100 项通过，脚本测试 84 项通过、1 项跳过，前端 52 项通过，生产构建、Ruff、Prettier 和修改组件 ESLint 通过。
- 临时 Git 仓库验证 npm 元数据放行且检查不改写文件、真实依赖变化仍受保护、强制更新覆盖暂存及未暂存修改和冲突的未跟踪文件。真实执行 Git、venv、pip、npm，验证三个实例恢复；故意构建失败时恢复程序版本和运行环境，但不恢复已丢弃的本地修改。
- 确认弹窗测试覆盖显示目标版本和实例数、只有二次确认才提交、取消或关闭不执行更新；强制标志只接受显式布尔值，不绕过工具和部署检查。同版本重新应用必须显式选择强制更新。
- 隔离本机预览重启加载新接口。此次浏览器自动化连接仍超时，未完成真实浏览器点击弹窗验证；前端行为由确认回调测试和生产构建验证。

2026-09-06 基于 `alpha` 的 `4ffb1f3d` 补充源码版本管理与 Python 环境验证：

- 后端全量 1120 项通过；脚本测试 84 项通过、1 项跳过；前端 57 项通过，生产构建、Ruff、Prettier 和修改组件 ESLint 通过。
- 10 项真实临时 Git 更新事务使用本地生成的两个版本测试 wheel，验证代码及实际依赖升级 / 回退、三个实例恢复、外部环境原地安装、构建失败后旧代码与依赖恢复。无 pip 环境分别使用 uv 0.12.10 指定解释器安装，以及 ensurepip 在停止实例前补齐 pip；未下载游戏依赖、未操作模拟器。
- 临时 SQLite 数据库验证仅删除 `saved_state`，保留 `mastery_plan`、库存和历史表；覆盖外部、相对数据目录、绝对实例路径及重复记录。
- 单元测试覆盖 Windows/Linux/macOS 源码识别、自定义 / 嵌套虚拟环境名、外部环境、Conda 目录及系统 Python，不以 `.exe` 后缀或 pip 是否存在推断环境类型。对应系统及完整 Conda 安装尚未实机测试。
- 分支 / tag / 短 SHA 检查固定到完整提交，兼容旧版本限制在提交前生效；前端测试覆盖切换选择后丢弃过期响应、二次确认、取消不提交及自动更新关闭说明。
- 本机隔离预览重新加载后，实连 GitHub 成功取得远端分支和最近 20 次提交，能检查旧提交 `a5c406b2`；`v4.1.6-alpha.3` 因缺少实例恢复模块被拒绝。未提交预览或正式实例的软件切换任务。浏览器自动化连接超时，本次新界面没有完成真实浏览器点击验证。

2026-09-06 基于 `alpha` 的 `f1d737c2` 补充准备阶段与独立进度验证：

- 后端全量 1132 项通过；补充网络超时取消用例及清理调整后，更新相关 67 项测试通过。脚本测试 84 项通过、1 项跳过；前端 57 项通过，生产构建、Ruff、Prettier 和修改组件 ESLint 通过。
- 13 项真实临时 Git 事务覆盖准备时原实例仍运行、构建中取消后原 PID / 本地修改保留、依赖未变化时跳过 pip 与环境重建、实际依赖安装失败后恢复，以及 pip / uv 的三个实例升级与回退。
- 使用本机 HTTP 端口验证进度页、token 认证、同源取消、过期任务拒绝及端口释放；实际点击独立进度页的取消按钮并截取进行中 / 已取消状态，截图使用临时示例任务。隔离预览重启后，新进度页与取消接口可访问。
- 未对正式本机或远端实例执行新版本替换；跨平台分支有测试覆盖，Windows/Linux 的完整更新及原生窗口仍需对应系统验证。

Windows/Linux 的包选择与便携目录替换有测试覆盖，但尚未进行对应系统的真实独立包启动测试。当前工作区和正式运行实例均未执行实际版本替换。

可重复运行的测试：

```bash
.venv/bin/python -m unittest \
  arknights_mower.tests.software_update_tests \
  arknights_mower.tests.software_update_transaction_tests \
  arknights_mower.tests.software_update_progress_tests \
  arknights_mower.tests.source_update_preflight_tests \
  arknights_mower.tests.source_version_tests \
  arknights_mower.tests.software_auto_update_tests \
  arknights_mower.tests.process_control_tests \
  arknights_mower.tests.socks_proxy_tests \
  arknights_mower.tests.mastery_restart_tests \
  arknights_mower.tests.manager_tests \
  arknights_mower.tests.webview_ui_tests
```
